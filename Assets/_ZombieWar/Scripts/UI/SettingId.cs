namespace ZombieWar.UI
{
    // One row of the settings sheet. The popup reports which switch moved; the binder decides
    // what that switch controls.
    public enum SettingId
    {
        Sound = 0,
        Music = 1,
        Haptics = 2,
        CameraShake = 3
    }
}
