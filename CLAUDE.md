# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## BẮT BUỘC: đọc `CODE-RULE.md` trước

Trước khi viết, sửa, hay review bất kỳ script nào trong project, phải đọc toàn bộ **`CODE-RULE.md`** ở root repo. Đó là bộ quy tắc code bắt buộc (cấu trúc thư mục, naming, kiến trúc data-driven, cấm bootstrap runtime, zero-allocation, SOLID, quy trình compile-check). File CLAUDE.md này chỉ chứa những gì riêng của project và không lặp lại các quy tắc đó. Code vi phạm `CODE-RULE.md` coi như chưa xong.

## Quy tắc ngôn ngữ

- **Mọi thứ trong script** viết bằng **tiếng Anh 100%**: tên class/method/field/biến, string log, comment, tên asset/prefab/scene do code tham chiếu. Không để tiếng Việt lọt vào code, kể cả comment.
- Trả lời người dùng bằng tiếng Việt.

## Project này là gì

**Zombie War** — game bắn súng 3D góc nhìn top-down cho mobile, làm để nộp bài test kỹ thuật, dựng trên một template game casual có sẵn. Toàn bộ asset và code first-party nằm dưới `Assets/_ZombieWar/` (xem Bố cục code first-party); mọi asset pack third-party gom trong `Assets/ThirdParty/`. Gameplay P0 đã dựng xong khung (xem "Hiện trạng hệ thống"). **Game chạy màn hình dọc 9:16** (quyết định của user ngày 2026-09-05, khác với spec docx viết cho landscape). **Game có đúng HAI scene**: `Assets/_ZombieWar/Scenes/Loading.unity` (build index 0, màn loading + thanh tiến độ, thêm 2026-09-06 theo yêu cầu user) và `Gameplay.unity` (toàn bộ game). Main menu là overlay trong `Gameplay`, **các level không tách scene** — map mỗi level là prefab `Prefabs/Environment/Map_*.prefab` được `LevelMapLoader` instantiate khi bắt đầu một lượt chơi.

## Spec (nguồn sự thật cho gameplay)

Spec/GDD đầy đủ (số cân bằng, state machine, wave timeline, acceptance criteria) là **`docs/Zombie_War_SPEC_GDD_V1.0.docx`** (đã thay thế `docs/GAME_SPEC.md` cũ). File docx không đọc trực tiếp được — trích text bằng Python (`zipfile` + parse `word/document.xml`, xuất heading/paragraph/table ra markdown vào scratchpad) rồi đọc. Theo `CODE-RULE.md` §8, logic gameplay phải bám đúng spec — không sáng tạo thêm cơ chế, mọi số cân bằng là nút vặn trong ScriptableObject. Đọc spec trước khi bắt đầu bất kỳ tính năng gameplay nào.

Bản rút gọn 8 yêu cầu bắt buộc:

1. **Camera** — top-down, **Cinemachine** follow soldier; zombie tràn về từ bốn phía.
2. **Control** — virtual joystick.
3. **Soldier** — Animator **layer** tách chạy (thân dưới) và bắn (thân trên); hiệu ứng mất máu nhìn thấy được.
4. **Zombie** — AI tìm soldier; phản hồi khi trúng đạn; chết dissolve bằng **shader**.
5. **Gun** — ít nhất 2 loại súng, nút switch trên HUD, particle nòng súng/đạn chạm, giật súng.
6. **Bom** — nổ gây sát thương **và lực vật lý** lên zombie.
7. **Level** — mỗi level ~3 phút. Level 1: phẳng + vật cản. Level 2 (điểm cộng): dốc + zombie khổng lồ. Nhịp spawn tăng dần.
8. **Âm thanh + particle** xuyên suốt.

Chấm theo Gameplay, Physics, Animation (Blend Trees), Shader/Visual/UI có hỗ trợ multi-resolution. Nộp: source GitHub, **APK Android**, video gameplay. Deadline 7 ngày (mục tiêu 3–4 ngày).

## Phiên bản Unity (quan trọng)

- Editor là **`2022.3.62f3`** (`ProjectSettings/ProjectVersion.txt`). Project đã chủ động hạ từ Unity 6 xuống; mở đúng phiên bản này để tránh bị ép upgrade. Nếu `README.md` và `ProjectVersion.txt` lệch nhau, `ProjectVersion.txt` là đúng.

## Package cho spec (đã cài)

- **Cinemachine `2.10.7`** — spec bắt buộc, dùng cho camera top-down follow soldier. Không tự viết camera follow.
- **AI Navigation `1.1.7`** — cung cấp `NavMeshSurface` để bake theo component; cần cho dốc ở Level 2 và né vật cản. Module built-in `com.unity.modules.ai` (`NavMeshAgent`) vẫn dùng song song như bình thường.

## Stack chính

- **URP 14.0** với hai renderer trên `Assets/Settings/UniversalRP.asset`: index 0 = `Renderer2D.asset`, index **1 = `UniversalRenderer.asset`** (3D forward) và **index 1 là mặc định** — camera để `Renderer: Default` là đã đi đúng đường 3D, không cần set tay. `UniversalRP.asset` cũng được gán vào Project Settings → Graphics → Scriptable Render Pipeline Settings, ngoài override sẵn ở cả 6 quality level. Cấu hình đang để: MSAA **4x**, HDR bật, `IntermediateTextureMode: Auto`, SRP Batcher bật, shadow distance 50 / 1 cascade. Global settings: `Assets/UniversalRenderPipelineGlobalSettings.asset`.
- **Input System mới 1.14.2** duy nhất (`activeInputHandler: 1`; `Input.*` cũ đã tắt). Asset action `Assets/InputSystem_Actions.inputactions` đã đăng ký trong `EditorBuildSettings` và có sẵn map `Player` (`Move`, `Attack`, `Previous`, `Next`, ...) — nối joystick ảo bằng component **On-Screen Stick/Button** của Input System vào các action này, không dựng đường input song song.
- **DOTween / DOTweenPro** (`Assets/Plugins/Demigiant/`), settings tại `Assets/Resources/DOTweenSettings.asset`. Luôn `SetLink(gameObject)`.
- **GUI PRO Kit - Sci-Fi Survival** (`Assets/ThirdParty/GUI PRO Kit - Sci-Fi Survival/`) — **nguồn art UI duy nhất của game** (quyết định user 2026-09-05). Button/frame/popup/slider/label/toggle, bộ pictogram `Icon_FunctionIcons(x2)/64` (~250 icon), item icon 128/256/512, art màn chơi `Demo_Play` (joystick, nút hành động, minimap) và background. 83 asset đã copy sang `Art/UI/` — xem "Kiến trúc UI".
- **Layer Lab GUI Pro-CasualGame** (`Assets/ThirdParty/Layer Lab/GUI Pro-CasualGame/`) — pack UI casual cũ, **đã bị thay hoàn toàn bởi Sci-Fi Survival**; 0 file được tham chiếu (rà GUID 2026-09-05), 180 MB. Chỉ còn giữ vì có script `UIParticleSystem`; gỡ được nếu cần chỗ.
- **Toony Colors Pro 2** (`Assets/ThirdParty/JMO Assets/`) — toon shading, có asmdef riêng `ToonyColorsPro.*`; nền tốt cho dissolve zombie và look toon.
- **TextMeshPro**, **Timeline**, **Visual Scripting**, **bộ 2D** đã cài nhưng không phải trọng tâm của game này. TMP Essential Resources **đã import** (`Assets/TextMesh Pro/`); font UI của game là **Oxanium ExtraBold** (title) + **Ubuntu Bold** (body) copy từ Sci-Fi Survival sang `Art/UI/Fonts/` (xem "Kiến trúc UI"), `LiberationSans SDF` chỉ còn là fallback mặc định của TMP.
- **DOTween modules có asmdef riêng** (`DOTween.Modules.asmdef`, `DOTweenPro.Scripts.asmdef`, tạo bằng chính ASMDEFManager của DOTween). `ZombieWar.asmdef` reference `DOTween.Modules` để dùng `DOFillAmount`/`DOFade` của uGUI; không xoá các asmdef này.

## Bố cục `Assets/` và asset third-party

Root `Assets/` chỉ còn: `_ZombieWar/` (first-party), `ThirdParty/` (kho asset pack ngoài), `Plugins/` (DOTween DLL — special folder của Unity, không được move), `Resources/` (DOTweenSettings), `Settings/` (URP), `TextMesh Pro/`, cùng 3 asset lẻ (`DefaultVolumeProfile.asset`, `UniversalRenderPipelineGlobalSettings.asset`, `InputSystem_Actions.inputactions`).

**Luật vàng: asset nào game thật sự dùng thì nằm trong `Assets/_ZombieWar/`, phân theo loại** (`Models/`, `Animation/`, `Audio/`, `Art/Materials`, `Art/Textures`, `Art/Shaders`, `Prefabs/`) — không tham chiếu xuyên qua `ThirdParty/`. Đợt gom 2026-09-05 đã kéo 183 file (~200 MB) từ 11 pack vào `_ZombieWar`, nên **`ThirdParty/` hiện là kho dự phòng thuần: 0 file trong đó được scene/prefab nào tham chiếu**.

Quy trình khi cần thêm asset từ pack: import pack vào `Assets/ThirdParty/<tên pack>/`, lấy đúng file cần dùng move sang thư mục loại tương ứng trong `_ZombieWar/`, phần còn lại để nguyên trong `ThirdParty/`. Move bằng Unity (`AssetDatabase.MoveAsset` / kéo trong Project window) để giữ GUID, đừng move bằng file system khi Editor đang mở.

Kiểm tra lại bất cứ lúc nào bằng cách rà GUID: quét chuỗi `guid:` trong mọi asset dưới `_ZombieWar/` + `Settings/` + `ProjectSettings/`, lấy bao đóng bắc cầu (transitive closure) rồi đối chiếu với `.meta` toàn project — file nào ngoài tập đó là không dùng.

Đợt dọn 2026-09-05 đã xoá vĩnh viễn (khôi phục được qua git nếu cần): demo scene + demo folder của Epic Toon FX / WarFX / Toony Colors Pro / Low Poly Guns / ToonSoldiers / Survivalist, `Epic Toon FX/Prefabs 2D` + `Upgrade`, `WarFX/_Effects` + `Desktop` (bản desktop), `Survivalist/Materials HDRP`, phần không dùng của `Survivalist/StarterAssets`, `ithappy/Military_Free/Render_Pipeline_Convert` (3 unitypackage convert pipeline), `Low Poly Guns/Scripts` (script demo Assembly-CSharp) và `Assets/Scenes/SampleScene.unity`.

## Asset pack — phân vai

Mỗi pack chỉ đóng **một** vai. Đừng lấy model của pack animation hay ngược lại.

### Model

Model đang dùng nằm trong `Assets/_ZombieWar/Models/`; cột "Pack gốc" chỉ để biết lấy thêm ở đâu và ghi công.

| File trong `_ZombieWar/Models/` | Vai | Pack gốc | Ghi chú |
|---|---|---|---|
| `Characters/Zombie.fbx` | Zombie chính | ArtStore3D | Rig **Humanoid**, `URP/Lit`. Material `Art/Materials/Characters/Zombie_Mat.mat` |
| `MeshReduce/Zombie_Reduced70.asset` | Mesh zombie đã giảm 30% (thêm 2026-09-06 theo yêu cầu user) | Sinh từ `Zombie.fbx` bằng **Mantis LOD Editor** (`Assets/MantisLODEditor/`) | Quality 70, Protect Boundary + Beautiful Triangles, các đỉnh không dùng đã nén lại: 3466 → 2434 tri, 2107 → 1503 đỉnh, giữ nguyên 62 bindpose/bone weight nên retarget Humanoid và dissolve MPB không đổi; `m_IsReadable 0` như FBX gốc. **`Zombie.prefab` (gốc của 4 variant Walker/Runner/Brute/Giant) trỏ SkinnedMeshRenderer vào mesh này**, không còn trỏ vào FBX; FBX vẫn giữ vì là nguồn avatar/rig. Muốn giảm lại thì chạy Mantis qua snippet MCP tạm (`Mantis_Mesh` + `PrepareSimplify`/`Simplify`/`SetQuality`) trên bản `Instantiate` của mesh FBX rồi `CreateAsset` đè cùng path để giữ GUID; thư mục `Models/MeshReduce/` là chỗ đặt mọi mesh giảm về sau |
| `Characters/SK_Military_Survivalist.fbx` + `Armature.fbx` | Soldier người chơi | Survivalist | Rig Humanoid. 13 material ở `Art/Materials/Characters/` dùng `URP/Autodesk Interactive` (pack import từ FBX Autodesk Interactive) — nặng hơn `URP/Lit` nhưng chỉ 1 instance trên màn hình |
| `Weapons/assault1.fbx`, `Weapons/shotgun1.fbx`, `Weapons/sniper2.fbx`, `Weapons/smg1.fbx` | 4 loại súng (spec §5 cần ≥ 2; sniper + SMG thêm 2026-09-06 theo yêu cầu user) | Low Poly Guns | Material ở `Art/Materials/Weapons/*.mat` là Toony Colors Pro 2 Hybrid (bản `URP/Lit` của pack chỉ còn trong `ThirdParty`). Sniper chọn `sniper2` (3.4k tri, không bipod) thay `sniper1` (4.8k tri, bipod xoè 17 cm). SMG dùng **`smg1`** theo yêu cầu user (48k tri, nặng nhất project nhưng chỉ một instance; bản `smg2` 2.2k tri đã thử rồi trả về `ThirdParty`). Scale trong prefab: rifle 0.1736, shotgun 0.1609, sniper 0.16, SMG 0.1736; vị trí `Model`/`Muzzle`/`GripLeft` suy từ hình học mesh (báng cầm đặt tại gốc prefab, trục nòng ở y ≈ 0.11 như rifle), chưa soát bằng mắt |
| `Environment/*.fbx` (45 mesh) | Môi trường, vật cản, set dressing | ithappy Military_Free | Prefab tương ứng ở `Prefabs/Environment/Props/`: mỗi prop rắn đã có sẵn `BoxCollider` (theo bounds renderer) + layer Obstacle + static flags, prop cao ≥ 2 m (`Tent_002/010`, `Tower_003`, `Radiostation_001`, `Helicopter_001`, `Tank_006`, `Target_002`) kèm `NavMeshModifierVolume` Not Walkable; cỏ `Grass_003/008` và xương rồng `Cactus_001/002` không collider. Dùng chung `Art/Materials/Environment/Military_base.mat` (+ `Picture_1/2.mat`). Đợt 2026-09-06 kéo thêm 31 mesh: bao cát dài `Barrier_007` (rào biên L1), tháp canh `Tower_003`, lều `Tent_010`, container radio, trực thăng (+2 cánh quạt), xe tăng (+10 bộ phận), máy phát, súng máy, cối, bàn, bia tập, lốp, cỏ, xương rồng. Còn lại trong `ThirdParty/ithappy/Military_Free`: Frame, Conditioner_002, Door_003, Machine_gun_004, Protection_001, Target_001, skybox |
| `Characters/ToonSoldier_WW2_demo.FBX` | Model preview clip | ToonSoldiers_WW2_demo | Đi kèm pack animation, giữ để preview |
| `VFX/Plane1x1.FBX` | Mesh phẳng cho particle | WarFX | |

### Animation

Tất cả rig nhân vật đều **Humanoid**, nên clip retarget chéo giữa các model được.

| File trong `_ZombieWar/Animation/` | Clip | Pack gốc |
|---|---|---|
| `Soldier/infantry_combat_idle.FBX`, `infantry_combat_shoot.FBX` | Idle + bắn cho layer thân trên (spec §3) | ToonSoldiers_WW2_demo |
| `Soldier/Locomotion--Run_N/Run_S/Walk_N.anim.fbx`, `Stand--Idle.anim.fbx` | Locomotion + Blend Tree thân dưới | Survivalist StarterAssets |
| `Zombie/Zombie_Idle_01`, `Walk_01`, `Walk_Fast01`, `Run_01`, `Attack01`, `HitReact_Head`, `HitReact_LeftArm`, `HitReact_RightArm`, `Idle_Death`, `T_pose` | Chase / attack / hit-reaction theo hướng đạn / death (spec §4) | Zombie_Animations |

Animation zombie đến từ pack `Zombie_Animations` (import 2026-09-05); 10 clip đang dùng đã nằm trong `_ZombieWar/Animation/Zombie/`, phần còn lại vẫn ở `ThirdParty/Zombie_Animations/` nếu cần lấy thêm. Pack `Zombie 1 Low Poly` chỉ có animation camera/đèn của demo scene; model đã rig Humanoid kèm Avatar nên clip Humanoid retarget thẳng được — spec §4 cần chase / hit-reaction / death.

Pack `FREE Shirtless Zombie` (`Assets/NewPunch/`) **đã bị gỡ khỏi project**: đối chiếu tận file `.unitypackage` gốc thì nó chỉ có 2 FBX model, 6 prefab, 3 scene demo, 1 script và material/texture — không có animation nào, trong khi vai trò dự kiến của nó là nguồn animation zombie. Đừng import lại.

`ThirdParty/Survivalist/StarterAssets/ThirdPersonController/Character/Animations/` còn kèm `StarterAssetsThirdPerson.controller` — Animator controller có sẵn Blend Tree locomotion, để lại đó làm tham khảo cho layer thân dưới của soldier.

### Sound

| Thư mục | Vai | Pack gốc |
|---|---|---|
| `_ZombieWar/Audio/Zombies/` | 8 wav zombie đang dùng (aggressive, death, growl, grunt, hiss, moan) | Tybug Studios Zombie Voice Pack |
| `_ZombieWar/Audio/Weapons/` | 8 wav súng (rifle, shotgun; sniper bắn `AntiMaterialRifle_3p_01`, SMG bắn `AutoGun_1p_01`; `Pistol_ClipIn_05` làm tiếng tap UI / ném bom / ném molotov; nổ bom đang mượn `AntiMaterialRifle_far_01`; `AssaultCanon_1p_tail` kiêm lửa bén (hazard + molotov) và sóng xung kích; `JackHammer_3p_01` kiêm tiếng sét của CHAIN LIGHTNING; drone bắn chung `AutoGun_3p_01` với rifle; `JackHammer_Reload` không còn ai dùng từ khi bỏ nạp đạn) | PostApocalypseGunsDemo |

Muốn thêm tiếng: 2 wav zombie còn lại ở `ThirdParty/Tybug Studios/`, 35 wav súng còn lại ở `ThirdParty/PostApocalypseGunsDemo/`. Project vẫn **chưa có nhạc nền** — không pack nào có track.

### Particle

Prefab particle của game là first-party trong `_ZombieWar/Prefabs/VFX/`, ăn 18 material ở `Art/Materials/VFX/` + 16 texture ở `Art/Textures/VFX/` + 2 shader `Art/Shaders/WFX_S Particle *.shader`. ⚠️ **Muzzle flash WarFX (`WFXMR_MF 4P *`) dựng dọc trục −X của prefab** (4 plane xoay quanh X, `Sparks` ở x = −0.3): trong `Vfx_MuzzleRifle/Shotgun.prefab` node con `WFXMR_MF 4P RIFLE1/3` xoay **Y = +90°** để lửa nằm dọc +Z của `Muzzle` (sửa 2026-09-05, trước đó flash xòe ngang vuông góc nòng). Root prefab phải giữ identity vì `PooledVfx.AttachTo` ghi local identity lên root; muốn chỉnh hướng thì chỉnh node con.

| Kho dự phòng | Vai |
|---|---|
| `ThirdParty/JMO Assets/WarFX/` | VFX súng đạn thực chiến. Chỉ còn bộ **`_Effects (Mobile)`** + `Mobile/`; bản desktop (`_Effects`, `Desktop/`) và scene demo đã xoá |
| `ThirdParty/Epic Toon FX/` | VFX toon — `Prefabs/Combat`, `Environment`, `Interactive`. **534 MB mà game mới lấy đúng 10 file** (5 material + 5 texture, đã chuyển sang `_ZombieWar/Art/`). Hiệu ứng zombie chết là `Prefabs/VFX/Vfx_ZombieDeathBlood.prefab` (đổi 2026-09-06 theo yêu cầu user — bản khói đầu lâu `PoisonDeath` bị loại vì lệch concept), dựng từ `Combat/Blood/Red/BloodExplosion` của pack: unpack, thêm `PooledVfx` (`_lifetime` 0.8), chỉ ăn `circle_AB.mat` đã có; gán trong `_deathVfx` của 4 `ZombieDefinition`, entry prewarm 16 trong `VfxService`. **`ZombieManager` phát nó trong `HandleDied` (đúng đòn kết liễu)**, không phải lúc despawn sau dissolve như trước — user 2026-09-06 thấy hiệu ứng hiện quá trễ. `Vfx_ZombieDeathSmoke` cũ vẫn còn vì SHOCKWAVE dùng nó làm `_burstVfx`. **Nổ bom cũng đổi sang pack này** (quyết định user 2026-09-06: ưu tiên Epic Toon FX cho particle dạng nổ — phạm vi lần này chỉ hiệu ứng nổ bom, muzzle/tracer/impact súng vẫn giữ WarFX vì hợp phong cách súng thật hơn): `Vfx_BombExplosion.prefab` dựng lại từ `Combat/Explosions/GrenadeExplosion/GrenadeExplosionFire` (bản màu lửa, không phải Blue/Green/Pink) thay cho `WFXMR_Explosion` cũ của WarFX. Root prefab (`GameObject` + `Transform` + `PooledVfx`) **giữ nguyên fileID** — chỉ xoá cây con WarFX và ghép cây Epic Toon FX (`GrenadeExplosionFire` + 4 con `Smoke/Glow/Fire/Sparks`) vào làm con mới — nên `BombDefinition._explosionVfx` và entry prewarm 3 trong `VfxService` (`GameplayRoot.prefab`) không phải sửa gì. `_rootParticle` trỏ vào ParticleSystem của node `GrenadeExplosionFire` (Play/Stop `withChildren: true` nên cascade xuống cả 4 con), `_lifetime` đo bằng `startDelay+duration+startLifetime` lớn nhất trong 5 particle system = 1.8 s. Component `AudioSource` kèm sẵn trong prefab pack (`explosion_dry` gì đó) đã bị xoá vì `BombThrower.Explode` tự phát `_explosionClip` qua `AudioService`. 3 material mới `glow1_ADD.mat` / `explosion_sharp_flat_3x3_AB.mat` / `circle_sharp_AB.mat` (+ texture cùng tên) move từ `Materials/Basics`, `Materials/Misc/Explosions`; node `Smoke` và root dùng lại `cloud_2x2_default_AB.mat` đã có sẵn trong `_ZombieWar` (không phải move). Cả 3 material pack này đã sẵn shader `URP/Particles/Unlit` từ trong pack, không cần convert tay như WarFX |
| `ThirdParty/GUI PRO Kit - Sci-Fi Survival/` | Sprite/font UI sci-fi — **nguồn art UI duy nhất**, 250 MB, đã lấy 83 file vào `Art/UI/` |
| `ThirdParty/Layer Lab/GUI Pro-CasualGame/` | Pack UI casual cũ — **180 MB, 0 file được dùng** sau khi đổi skin sang Sci-Fi Survival |

Material built-in của pack mới import convert bằng converter chính chủ của Unity (chọn material → **Edit ▸ Rendering ▸ Materials ▸ Convert Selected Built-in Materials to URP**); skybox và material UI cố ý không đụng tới vì chạy tốt dưới URP. Tool gom-material tự viết trước đây đã xoá (xem "Không giữ editor tool sinh object" ở mục Bố cục code). URP **không có** upgrader cho `Mobile/Particles/*` và `Legacy Shaders/Particles/*`. 14 material particle của WarFX đã gán tay sang `URP/Particles/Unlit` (`_BaseColor = 2 × _TintColor` vì shader particle đời cũ nhân đôi tint; additive → `_Blend: 2`, alpha blended → `_Blend: 0`).

**35 material trong `Layer Lab/GUI Pro-CasualGame/ResourcesData/Particle/Materials/` cố ý giữ nguyên `Mobile/Particles/*`** — đó là particle UI chạy qua `UIParticleSystem` trên `CanvasRenderer`, không phải `ParticleSystemRenderer`. Shader particle của URP không hỗ trợ masking/stencil của Canvas, đổi sang là hỏng UI. Chúng render đúng dưới URP như hiện tại.

## Bố cục code first-party

Khung thư mục đã dựng sẵn theo `CODE-RULE.md` §1 và phân rã hệ thống ở `docs/GAME_SPEC.md` §4:

```
Assets/_ZombieWar/            # mọi thứ first-party nằm trong đây, tách hẳn khỏi asset pack third-party
├── Scripts/    Core, Player, Weapons, Enemies, Level, Roguelike, UI, Data, Audio, VFX, Utils   # không có Editor/
├── Data/       Weapons, Zombies, Levels     # instance .asset của ScriptableObject
├── Prefabs/    Player, Enemies, Weapons, VFX, UI, Environment
├── Animation/  Soldier, Zombie              # Animator controller, Avatar Mask, Blend Tree, clip FBX
├── Models/     Characters, Weapons, Environment, VFX   # FBX đang dùng, kéo từ pack về
├── Audio/      Weapons, Zombies             # wav đang dùng
├── Art/        Materials, Textures, Shaders, UI        # material/texture/shader/art UI đang dùng
└── Scenes/                                  # scene gameplay 3D
```

`CODE-RULE.md` §1 viết đường dẫn là `Assets/Scripts/`; project này đặt cả cây dưới root `Assets/_ZombieWar/`, cấu trúc con và luật chia hệ thống giữ nguyên. Scene gameplay tạo trong `Assets/_ZombieWar/Scenes/`.

Thư mục còn rỗng giữ bằng `.gitkeep` (Unity bỏ qua file bắt đầu bằng dấu chấm, không sinh `.meta`) — xoá khi thư mục có asset thật.

Một assembly definition duy nhất tách code first-party khỏi `Assembly-CSharp` của các asset pack: `ZombieWar` (`Assets/_ZombieWar/Scripts/ZombieWar.asmdef`, runtime, reference Unity.InputSystem / Cinemachine / Unity.AI.Navigation / Unity.TextMeshPro / UnityEngine.UI). DOTween là DLL trong `Assets/Plugins/` nên auto-reference, không cần khai báo. Thêm package mới cần dùng trong script thì bổ sung vào `references` của `ZombieWar.asmdef`. Không bao giờ đặt code vào `Assets/ThirdParty/JMO Assets`, `Assets/ThirdParty/Layer Lab`, `Assets/Plugins`.

**Không giữ editor tool sinh object** (quyết định user 2026-09-05): project **không có** thư mục `Scripts/Editor/` hay assembly `ZombieWar.Editor`. Mọi script kiểu builder/installer/importer/converter (dựng `UIRoot.prefab` + prefab popup, wire `GameplayRoot`, sinh `Skill_*.asset`, build Animator controller từ recipe, convert material, copy asset UI từ pack) đã xoá cùng `Animation/AnimatorBuildRecipe.asset`; muốn tra lại cách chúng dựng thì xem lịch sử git. **Prefab và `.asset` đang có là nguồn sự thật** — chỉnh trực tiếp trong Editor hoặc qua Unity MCP. Khi thật sự cần dựng hàng loạt, viết tool tạm, chạy xong thì xoá trước khi kết thúc task, không commit tool. Chỉ khi cần một editor script *thường trực* (ví dụ build APK headless) mới tạo lại `Scripts/Editor/` + asmdef riêng cho đúng thứ đó.

`Assets/_Recovery/0.unity` là file recovery sau crash của Unity, không phải scene thật — không dựng gì trên nó; xoá khi tiện.

## Hiện trạng hệ thống (cập nhật 2026-09-05)

Toàn bộ runtime nằm trong một prefab **`Assets/_ZombieWar/Prefabs/GameplayRoot.prefab`** (Systems/Managers, Pools, Map, Player, CameraRig, và một instance `UIRoot.prefab`); scene `Gameplay` chỉ chứa một instance của nó.

**Vòng đời một lượt chơi (Loading → Gameplay):**

0. Mở app → scene `Loading` (`Core/LoadingSceneController` trên object `Loading`) gọi `SceneManager.LoadSceneAsync("Gameplay")` với `allowSceneActivation = false`, đẩy tiến độ (`progress / 0.9`, làm mượt bằng `_fillPerSecond`) vào `UI/LoadingView` (fill chạy bằng anchor như health bar, chữ % chỉ set khi đổi, dòng LOADING nhấp nháy DOTween `SetUpdate(true)`), và chỉ activate khi thanh đầy **và** đã hiện tối thiểu `_minimumDisplaySeconds` 1.2 s. Scene này tự chứa (camera solid color cullingMask 0 + `Canvas_Loading` 1080×1920 match width + `SafeAreaFitter`), không tham chiếu prefab gameplay nào; art lấy từ `Art/UI/` (nền `Background_10` tint navy, vignette `Background_ScreenGlow`, thanh `Slider11_Blue1_Frame`/`Slider11_Blue3_Fill`, icon `Icon_Skull`, font Oxanium/Ubuntu).

1. Vào scene → `GameFlowController` ở state **`Menu`**: chưa có map, `Player` **tắt** (`LevelMapLoader.Awake` tắt nó), `MenuUiBinder` hiện menu overlay.
2. Bấm PLAY một thẻ level → `MenuUiBinder` → `GameFlowController.StartRun(level)`: `LevelMapLoader.Load(level)` instantiate `LevelDefinitionSO.MapPrefab` vào `Map/`, bật `Player` và đặt tại `LevelMap.PlayerSpawn`, cắt blend camera (`PreviousStateIsValid = false`); phát `OnRunStarted` để `WaveDirector` nạp phase và `ZombieManager` prewarm pool; chuyển state `Countdown`.
3. Retry / Next / Main Menu → `LevelLoader.Reload` **load scene `Loading`** (`_loadingSceneName`), Loading lại load `Gameplay` mới. `Data/Levels/LevelSelection.asset` (`LevelSelectionSO`) mang level + cờ `AutoStart` qua lần reload: có cờ thì `GameFlowController.Start()` vào thẳng `StartRun`, không có thì về menu. `ConsumeAutoStart()` xoá cờ ngay khi đọc để lần Play sau trong Editor không tự nhảy vào trận.

Đi qua Loading để nhận một scene `Gameplay` mới là cách rẻ nhất để pool, physics và map chắc chắn sạch giữa hai lượt; chỉ "bấm PLAY từ menu" là khởi động tại chỗ vì lúc đó chưa có gì để dọn. Tên scene là hai string serialize (`LevelLoader._loadingSceneName` = "Loading", `LoadingSceneController._gameplaySceneName` = "Gameplay") — đổi tên scene thì sửa cả hai chỗ trong scene tương ứng.

⚠️ **Prewarm pool zombie phải xảy ra sau khi map tồn tại** — `NavMeshAgent` bật lên khi chưa có NavMesh sẽ log "Failed to create agent because there is no valid NavMesh" và không bao giờ bám mesh. Vì vậy `ZombieManager` tạo pool trong `OnRunStarted`, không phải `Awake`.

Map prefab (`Prefabs/Environment/Map_FlatOutpost.prefab`, `Map_BurningHills.prefab`) có root mang `LevelMap` + `NavMeshSurface` (collect **Children**, layer mask Obstacle+Ground, geometry = Physics Colliders, data bake ở `Prefabs/Environment/NavMesh_Map_*.asset`) và các con `Environment`, `Directional Light`, `PlayerSpawn` — cấu trúc bên trong `Environment`, hai bẫy NavMesh và quy trình bake lại nằm ở mục "Map và biên map" bên dưới. Thêm level mới = thêm map prefab + `LevelDefinitionSO` rồi kéo nó vào mảng `_levels` của `MenuUiBinder` trong `GameplayRoot.prefab` — trang Battle là carousel một thẻ nên không cần thêm slot UI, không đụng code.

Camera portrait: vcam pitch 72°, follow offset (0, 14, −4.5); `CameraAspectAdapter` giữ **FOV ngang** cố định (40°) và suy ra FOV dọc theo aspect (clamp 45–70°), nên 9:16 / 9:19.5 / 3:4 đều thấy cùng bề rộng làn. Spawn ring đã nới lên 14–20 m (min 12) vì màn dọc nhìn xa về phía trước; `_navMeshSampleRadius = 5` ở cả hai level để spawn được khi player đứng trên plateau. UI: Canvas Scaler reference 1080×1920, match 0.5. Vcam **follow `Player/CameraTarget`** (child rỗng) chứ không follow `Player`: `Core/CameraLookAhead` trên `CameraRig` đẩy target về phía joystick tối đa `_maxOffset` 1 m (SmoothDamp 0.4 s) — xem "Game feel".

Rung camera: Player có **ba** `CinemachineImpulseSource` — súng dùng source shape **Recoil** 0.12 s (`WeaponController._impulseSource`), bom dùng source shape Bump 0.2 s (`BombThrower._impulseSource`), bị cắn dùng source Bump 0.15 s (`PlayerHitReactionPresenter._impulseSource`); lực lấy từ `_cameraImpulse` trong `GunDefinitionSO` (rifle 0.035, shotgun 0.07) / `BombDefinitionSO` (0.55) / `PlayerDefinitionSO._hitCameraImpulse` (0.2). Mọi impulse đổ về một `CinemachineImpulseListener` trên `VCam_Follow`; `Core/CameraShakeController` (trên `CameraRig`) là công tắc duy nhất: đọc `SaveService.CameraShakeEnabled` (PlayerPrefs `zw_camera_shake`) và ghi `listener.m_Gain` = `_enabledGain` hoặc 0. Toggle "SCREEN SHAKE" có ở cả popup Pause (nối qua `GameplayUiBinder` → `UIManager.ShowPausePopup(..., shakeEnabled, onShakeChanged)`) lẫn popup Settings ở menu (xem "Settings").

| Hệ thống | Script chính | Ghi chú |
|---|---|---|
| Flow | `Core/GameFlowController` (Countdown→Playing→Paused/LevelUp/Won/Lost), `LevelTimer`, `ScoreTracker`, `SaveService` (PlayerPrefs) | Entry point duy nhất mỗi scene; UI chỉ subscribe event |
| Loading | `Core/LoadingSceneController` + `UI/LoadingView` (scene `Loading.unity`, không nằm trong `GameplayRoot`/`UIRoot`) | Load `Gameplay` async sau thanh tiến độ; xem bước 0 của vòng đời ở trên |
| Player | `Player/PlayerMotor` (Rigidbody), `PlayerAim` (OverlapSphereNonAlloc + Linecast LOS, hold 0.35 s, đổi target chỉ khi ứng viên tốt hơn `_targetSwitchMargin` 25%), `PlayerHealth`, `PlayerAnimationPresenter`, `PlayerHitFlash` (MPB, property `_Color` vì material Autodesk Interactive), `PlayerDeathPresenter` (tween ngã, vì không có clip death), `PlayerHitReactionPresenter` (máu + impulse + jolt khi bị cắn), `WeaponHandIk` (IK hai tay bám grip súng + cuộn ngón — xem hàng Weapons) | Input: `PlayerInputReader` đọc action `Player/Move`; joystick là On-Screen Stick `<Gamepad>/leftStick` |
| Weapons | `Weapons/WeaponController` (FSM Ready/Firing/Cooldown/Switching — **không có băng đạn / nạp đạn**, quyết định user 2026-09-05: súng bắn vô hạn, chỉ giới hạn bởi `FireInterval`), `Gun` (stats + motion recoil/switch thủ tục), `ProjectileManager` (SphereCast, pool 180), `BombThrower` + `Bomb` (Rigidbody, fuse, telegraph ring thở, trail, falloff, event `OnExploded`), `MolotovThrower` + `Molotov` + `MolotovBurn` (chai lửa của skill MOLOTOV, tái dùng prefab `FireZone`), `ThrowSolver` (điểm rơi + vận tốc ném dùng chung cho bom và molotov) | **Súng không treo vào tay mà treo vào thân** (sửa 2026-09-06): `GunSocket` là con của `Player/ModelPivot` tại **(0.13, 1.27, 0.14)** trong không gian thân (trước vai phải), hai bàn tay được **IK kéo tới súng** bằng `Player/WeaponHandIk` (trên `Model`, cùng object với Animator; layer `Full Body` bật IK Pass) — mỗi prefab `Gun_*` có `RecoilPivot/GripRight` + `GripLeft` là *pose xương bàn tay* (vị trí cổ tay + hướng: X xương = hướng đốt ngón, −Y = lòng bàn tay phải / +Y = lòng bàn tay trái, ±Z = ngón cái), gán vào `Gun._rightHandGrip/_leftHandGrip`; hint khuỷu tay ở `ModelPivot/IkHints/ElbowRight` (0.36, 1.08, 0.02) và `ElbowLeft` (−0.22, 1.10, 0.12). Ngón tay cuộn quanh báng bằng `SetBoneLocalRotation` (euler `_rightFingerCurl`/`_leftFingerCurl` (0, 0, −60) cộng lên pose nghỉ cache ở `Awake`). Lý do: clip combat của ToonSoldiers retarget sang rig Survivalist cho bàn tay xoè, chĩa lên-trái ~30°, còn nòng lại bị ép nằm ngang theo thân nên súng dính cạnh tay và tay trái không chạm súng; treo súng vào thân + IK tay thì clip chỉ còn quyết định thân và khuỷu. ⚠️ Hai bẫy của Animator IK: (1) **goal rotation của bàn tay không phải rotation của xương** — avatar humanoid định nghĩa một khung tay chuẩn lệch hằng số so với xương, `WeaponHandIk` đo hằng số đó một lần: frame IK đầu pin tay bằng đúng `Grip.rotation`, `LateUpdate` cùng frame đọc `Inverse(goal) * bone.rotation` rồi khử trên mọi goal sau; (2) **trong `OnAnimatorIK` transform xương còn là pose frame trước/bind pose** và `GetIKRotation` gọi ngoài IK pass trả rác kèm warning, nên không đo được offset ở hai chỗ đó. `AlignBarrel` được gọi thêm trong `OnAnimatorIK` (trước khi pin) để tay bám đúng pose nòng của frame hiện tại. **Gốc prefab `Gun_*` nằm đúng tại báng cầm** (`Model`/`Muzzle` trong prefab đã dời: rifle +0.05 y / +0.06 z, shotgun −0.01 y / +0.13 z) nên xoay nòng theo target là xoay quanh báng, súng không rời khỏi tay. `Muzzle` đặt trên **trục nòng** (rifle y 0.105, shotgun y 0.084), không phải y = 0 — trước đó nó thấp hơn nòng 6,5–9 cm nên flash và đạn lệch xuống. **Nòng được nắn lại mỗi frame** bằng `WeaponController.AlignBarrel()` (public, gọi trong `OnAnimatorIK` của `WeaponHandIk` và lại trong `WeaponController.LateUpdate` — cùng input nên lần hai vô hại): `Gun.AlignBarrelAt` xoay root súng cho nòng nằm ngang và chĩa vào target trong giới hạn `_aimYawLimitDegrees` ±25° quanh hướng thân (không target thì theo hướng thân); root súng giờ đứng yên trên `ModelPivot` nên nòng không còn ăn lắc của tay, và IK kéo tay theo nòng chứ không ngược lại. Đạn vẫn bay theo `Muzzle.forward` đúng spec §5.2, giờ nòng đã đúng chỗ. **Toàn bộ FSM bắn chạy trong `LateUpdate` sau bước nắn nòng** (không có `Update`): đạn, tracer và tia lửa spawn đúng pose nòng mà frame đó render, không phải pose frame trước. Muzzle flash gọi `VfxService.Play(prefab, Transform anchor)` — instance được `PooledVfx.AttachTo` gắn vào `Muzzle` suốt vòng đời (particle của WarFX đều Local space) rồi `Detach` về pool khi hết giờ, nên vừa chạy vừa bắn tia lửa không tụt lại phía sau nòng |
| Enemies | `Enemies/ZombieManager` (pool theo `ZombieDefinitionSO`, registry Collider→zombie, tick tập trung, tên layer xác), `ZombieController` (FSM Spawning/Chase/Attack/HitStun/Knockback/Dying; chết vì bom thì xác bay trên layer `Corpse`), `ZombieAnimationPresenter` (`HitSide`), `ZombieMaterialFx` (MPB `_HitAmount`/`_DissolveAmount`) | Shader `Art/Shaders/ZombieDissolve.shader` (HLSL URP, có ShadowCaster/DepthOnly) |
| Feel | `Core/HitStopController`, `Core/HitVignettePresenter`, `Core/CameraLookAhead`, `Volume` + `Art/Rendering/GameplayPostProcess.asset` | Xem "Game feel" bên dưới |
| Level | `Level/LevelMapLoader` + `LevelMap` (map prefab, spawn, NavMesh), `WaveDirector` (phase, cap, weighted pick, scripted Giant @145 s, anti-spike), `SpawnPointResolver`, `FireHazardSpawner` + `FireZone` (P1, Level 2 @75 s/@120 s) | L1 sân bê tông 56×56 m trong rào bao cát; L2 60×60 m, plateau 3.5 m + 4 dốc 22° trong vành đá — xem "Map và biên map" |
| UI | `UI/UIManager` (hub), `UI/Hud/*View`, `UI/Popup/{PopupBase,PopupManager,PopupBackdrop,PausePopupUI,ResultPopupUI,SkillChoicePopupUI,SkillCardView}`, `UI/MainMenu/{MenuScreenView,MenuHeaderView,MenuTabBarView,MenuTabButtonView,BattlePageView,LevelCardView,WeaponPageView,WeaponCardView,SkillTreePageView,SkillTreeNodeView,SkillDetailPanelView}`, `SwitchToggleView`, `SafeAreaFitter`, `UiButtonFx`, `TimeTextFormatter` (zero-alloc); ref gameplay nằm ở `Core/GameplayUiBinder` + `Core/MenuUiBinder` | Mọi màn hình trong `Prefabs/UI/UIRoot.prefab` — xem "Kiến trúc UI" |
| Roguelike | `Roguelike/RoguelikeDirector` (XP → battle level → draft), `BattleXpTracker`, `SkillDraft`, `Player/{PlayerStatSheet,AbilityRunner,AbilityContext,OrbitBladesController,ChainLightningCaster,ShockwaveEmitter,SentryDroneController}`, `Data/{PassiveSkillSO,ActiveSkillSO + 6 subclass,RoguelikeSettingsSO,StatId,StatModifier}` | Xem "Roguelike trong trận" bên dưới |
| Meta | `Core/ProfileService` (level/XP/coin + cấp nâng cấp từng súng + rank skill tree, lưu qua `SaveService`), `Data/ProgressionRulesSO`, phần `Upgrade` của `GunDefinitionSO` (`GetStats(level)` → struct `GunStats`, `GetUpgradeCost`) | Xem "Meta progression" bên dưới |
| Skill tree | `Data/{SkillTreeSO,SkillTreeNodeSO}` + `Data/SkillTree/*.asset`, `Core/SkillTreeProgress` (C# thuần), `Player/SkillTreeStatApplier`, lớp "permanent" trong `PlayerStatSheet`, `UI/MainMenu/{SkillTreePageView,SkillTreeNodeView,SkillDetailPanelView}` | Xem "Skill tree" bên dưới và `docs/SKILL-TREE.md` |
| Data | `Data/*SO` + instance trong `Assets/_ZombieWar/Data/{Player,Rules,Weapons,Zombies,Waves,Levels,Roguelike}` | Số liệu gốc chép từ spec §5–§10; chỉnh ở đây, không sửa code. **Đã lệch spec theo yêu cầu user 2026-09-05:** cap zombie mỗi phase +50% và spawn interval −25% (L1 18/33/48/66/80, L2 24/39/57/75/90), `_moveSpeed` giảm ~35% (Walker 1.5, Runner 2.3, Brute 1.1, Giant 1.0 — lần đầu −25%, hạ thêm ~13% ngày 2026-09-05 vì user thấy còn nhanh; ngưỡng blend animator 1.4/2.3/3.6 m/s giữ nguyên nên Walker chạy clip Walk, Runner đúng clip WalkFast, Brute/Giant blend Idle↔Walk), `PlayerDefinition._moveSpeed` 4.6 → 4.0 (cùng đợt, −13%), và **cân bằng lại súng + HP zombie ngày 2026-09-06** (user: giữ nhịp dồn dập nhưng không khó, không nhàm; rồi yêu cầu thêm **giảm sức mạnh súng, giảm HP enemy** và **rifle chỉ bắn trong khung hình**). Ý đồ: fodder vẫn chết trong 1–2 phát để màn hình liên tục "nổ", nhưng DPS tổng thấp để đám đông dồn lại và skill roguelike / active (Lightning 40, bom 160→60, Molotov, Blades 14, Shockwave 30 — số này không đổi) có sức nặng rõ hơn so với HP mới. Rifle `_damage` 16 → **12**, `_fireInterval` giữ **0.2** (5 phát/s, **60 dps**), `_range` 11 → **7**, `_projectileSpeed` 32 → **36**; shotgun `_damage` 12 → **10** × 9 pellet, `_fireInterval` 1.0 → **0.9**, `_range` 7 → **5**, `_projectileSpeed` 26 → **28**; `Gun_Drone._damage` 10 → **8** (range 12 giữ vì chỉ là tuổi thọ đạn, drone tự quét 10 m). **Hai súng thêm 2026-09-06** đặt trên cùng baseline: **Sniper Rifle** (`Gun_Sniper`, id `sniper`) `_damage` **48** / `_fireInterval` **0.9** / `_range` **8** / `_projectileSpeed` 60 / `_spreadAngle` 0.5 / `_knockback` 3.5 / **`_pierce` 2** (field mới trong `GunDefinitionSO`, cộng vào passive `ProjectilePierce` trong `WeaponController.Fire`) — một phát hạ Walker/Runner, hai phát hạ Brute, xuyên qua tối đa 3 thân; range giữ ≤ 8 để không bắn ra ngoài khung như rifle, bản sắc của nó là damage + xuyên chứ không phải tầm. **SMG** (`Gun_SMG`, id `smg`) `_damage` **7** / `_fireInterval` **0.1** (70 dps, 4 viên hạ Walker) / `_range` **6** / `_projectileSpeed` 34 / `_spreadAngle` 5 / `_knockback` 0.3. Cả hai `_unlockedByDefault` true, max level 5, giá gốc 220 / 150; muzzle: sniper mượn `Vfx_MuzzleShotgun`, SMG dùng `Vfx_MuzzleRifle`; `_animatorWeaponType` 0 (tham số `WeaponType` không có transition nào đọc). Nút switch trên HUD xoay vòng Rifle → Shotgun → SMG → Sniper. HP zombie: Walker 48 → **24** (2 viên rifle / 3 pellet), Runner 32 → **14** (2 viên, còn 1 viên sau một HIGH CALIBER), Brute 140 → **84** (7 viên ≈ 1.4 s; shotgun 9 × 10 = 90 nên **một phát trọn ổ sát mặt hạ Brute** — đúng luật risk/reward spec §6), Giant 1600 → **720** (~12 s rifle nền). Damage cắn / tốc độ / XP / score và toàn bộ wave giữ nguyên. **Tầm bắn theo khung hình**: camera ở player + (0, 14, −4.5), pitch 72°, FOV dọc 65.8° ở 9:16 → thấy **12.7 m phía trước, 8.2 m phía sau, ±5.35 m hai bên** tại hàng của player (±6.8 m ở mép xa). Vòng auto-aim là hình tròn nên 7 m nằm gọn phía trước và chỉ lố ~1.6 m ở hai mép bên; muốn tuyệt đối không bắn ngoài màn thì hạ về 5 hoặc thêm kiểm tra viewport trong `PlayerAim` (chưa làm). HP đổ vào mỗi giây (nhịp spawn × HP trung bình) L1 ≈ 32 / 41 / 59 / 83 / 123 so với rifle nền 60 dps: hai phase đầu chỉ cần súng, từ phase 3 dựa vào skill (đến 65 s đã có ~5 lần chọn). Nếu thấy phase 4–5 quá gắt, nút vặn nên chỉnh là `_aliveCap` phase 5, không phải damage súng. Tầm bắn cũng là bán kính auto-aim (`WeaponController` → `PlayerAim.SetScanRange`) và tuổi thọ đạn (`Projectile.RemainingLife = Range / ProjectileSpeed`); pool prewarm Walker/Runner/Brute 64/48/22, và `PlayerDefinition._anglePenaltyWeight` 0.35 → 0.02 để auto-aim ưu tiên zombie gần nhất (góc chỉ phá hoà); bù lại thêm `_targetSwitchMargin` 0.25 (ngoài spec) để trong đám đông nhân vật không quay qua lại giữa hai con gần ngang nhau |

### Map và biên map (dựng lại 2026-09-06 theo yêu cầu user)

User thấy map cũ (36×36 / 40×40 m, plane màu phẳng không texture, tường vô hình + dải đen, ngoài mép là khoảng trống) "rất prototype" và muốn rộng hơn. Cả hai map đã dựng lại, **lệch spec §10 có chủ ý**: L1 **56×56 m** (tường ±28), L2 **60×60 m** (tường ±30); spawn ring 14–20 m giữ nguyên. Camera pitch 72° nhìn xa tối đa ~14 m trước mặt và ~8.5 m sang bên, nên nền phải trải **≥ 20 m ra ngoài tường** để không bao giờ lộ background — vì vậy nền là một plane 112 m (L1) / 120 m (L2) và cảnh ngoài rào được dựng thật.

Cấu trúc `Environment` (giống nhau ở hai map, tên nhóm cố định):
- **L1**: `Ground/Skirt` là plane nền lớn 112 m, layer Ground, `BoxCollider` thay MeshCollider, mang **`NavMeshModifier ignoreFromBuild`** nên nền ngoài không bao giờ vào NavMesh; `Ground/Arena` là object rỗng layer Ground mang `BoxCollider` 56×56 (mặt trên y = 0) — **nguồn NavMesh duy nhất của sàn**; thêm 16 tile Plane 14 m (`Ground_Outpost`, sân bê tông) chỉ để render, cao hơn skirt 1 cm. **L2 (dựng lại 2026-09-06)**: địa hình là **hai mesh heightfield sinh thủ tục** ở `Models/Environment/Terrain_BurningHills_Arena.asset` (60×60 m, lưới 0.25 m trong ±22 / 0.5 m ngoài, 43.7k đỉnh, 2 submesh: đất `Ground_Hills` + vách `Hill_Cliff` cho mặt có pháp tuyến y < 0.6) trên `Ground/Arena` (MeshFilter + MeshRenderer + **MeshCollider**, layer Ground — nguồn NavMesh duy nhất) và `Terrain_BurningHills_Outer.asset` (vành 30–60 m, lưới 1 m, khoét lỗ đúng bằng sân nên hai mesh khớp đỉnh ở |x|,|z| = 30, 29.5k đỉnh) trên `Ground/Dirt` (MeshCollider, `ignoreFromBuild`). Hàm cao độ h(x,z) = max(plateau, 4 dốc) + 8 gò thấp + vành đồi ngoài + noise Perlin, cộng 2 pad phẳng ngoài rào: **plateau** hình vuông bo góc nửa cạnh 6 m, bán kính góc 2.5, cao 3.5, mép trên bo tròn r 0.8, vách gần thẳng đứng (rơi 2.2 m trong 0.25 m → ~84°, NavMesh 45° không leo, Rigidbody player cũng không bò lên được), chân vách bo r 0.5; **4 dốc** dài 13.5 m (user 2026-09-06: dốc 22° cũ quá gắt và cạnh vuông) với profile ease 10% hai đầu → giữa dốc ~16°, đầu/cuối tròn, bề rộng phẳng ±3 m + vai thoải 2 m; **8 gò** cao 0.7 m bán kính 5 tại (±13, ±13) và 0.5 m bán kính 4 tại (±24, 0)/(0, ±24) làm chân cho cụm đá; **vành đồi** ngoài tường dâng 0→3 m trong 31–36.5 m rồi cộng Perlin 0–2.5 (bước sóng 14) và 0–2 (bước sóng 25) tới 60 m; **pad** phẳng cho xác trực thăng (14, −41) r 7 cao 2.2 và trại (−42, 22) r 6 cao 2.0. Mesh là nguồn sự thật — hàm sinh không giữ trong repo, muốn đổi hình thì viết lại snippet MCP theo mô tả này (rebuild tại chỗ bằng `mesh.Clear()` để giữ GUID). Không còn `Terrain` group hay `NavMeshModifierVolume` cho plateau vì heightfield là một tấm duy nhất, không có lòng rỗng.
- `Borders`: 4 `BoxCollider` vô hình layer Obstacle ôm sát mép (L1 cao 1.5 m để đạn chạm ngang tầm bao cát, L2 cao 3 m).
- `Fence` (L1): 43 khúc bao cát `Barrier_007` ở ±28.25 (mặt trong trùng tường), cổng phía nam chắn bằng Hummer + 2 hedgehog. `Rocks` (L2, 67 instance): đá là 4 prefab **`Props/Rock_A..D`** (mesh `Models/Environment/Rock_A..D.asset` — icosphere subdivide 2 dời đỉnh bằng Perlin, đáy cắt phẳng, flat-shaded 960 đỉnh, ~2.1×1.1×1.9 m ở scale 1, `BoxCollider` theo bounds, layer Obstacle, material `Hill_Rock`; thay cho các cặp Cube nghiêng cũ vì user thấy cạnh vuông) đặt xoay theo pháp tuyến địa hình, chôn 0.12·scale: 3 viên trên mỗi gò góc (viên chính scale 2.0–2.3), 2 viên trên mỗi gò cạnh, 30 viên scale 1.4–3 rải trên vành đồi 32.5–38.5 m và 18 viên scale 2.5–4.5 trên đồi 41–55 m (random seed 20260907, tránh 2 pad).
- `Obstacles`: vật cản trong sân, tâm sân trống bán kính ~6 m. **L1 bố trí theo khu chức năng** (dựng lại 2026-09-06 vì user thấy bản trước "đồ đạc nhỏ rải rác không chủ ý"): 46 prop gom thành **lều chỉ huy** phía bắc (Tent_002 tại (0, 20) + bàn, máy phát, 3 thùng phuy, 2 hòm đạn), **bãi xe** phía đông (2 Hummer đỗ song song tại x 17.5/21.5, đống lốp, thùng phuy, thùng gỗ), **trường bắn** phía nam (ụ bao cát Barrier_006 tại (0, −17) + súng máy + cối + 3 bia tập xếp hàng ở z −23), **kho hậu cần** phía tây (lưới 2×2 Box_022 tại (−18.7, 4.7) + 1 thùng chồng lên, 2 Box_003, 4 thùng phuy 2×2, 2 khối bê tông), **4 ụ bao cát góc** tại (±20, ±20) quay mặt vào tâm (NE/SW kèm súng máy, NW/SE kèm hedgehog ngoài ụ) và 2 ụ bao cát chéo tại (9, −9)/(−9, 9) làm chỗ nấp giữa sân. Không còn thùng/lốp/hedgehog đơn lẻ nằm giữa làn. **L2 (28 prop, dựng lại 2026-09-06)**: đỉnh plateau chỉ có cụm cối + 2 hòm đạn ở góc NE và 2 thùng phuy góc SW để người chơi còn chỗ xoay; bốn góc thấp mỗi góc một chuyện — NE **đoàn xe bị hạ** (Tank_006 tại (21, 20), Hummer bên cạnh, 3 thùng phuy, thùng gỗ), NW **ụ súng máy trên gò đá** (Barrier_006 + súng máy + hòm đạn tại (−13, 13), 3 hedgehog xếp hàng chéo ngoài gò), SE **trại tạm** (Tent_002 tại (20.5, −20.5), máy phát, thùng phuy, thùng gỗ), SW **bãi bê tông** (3 khối Barrier_015/004 cạnh gò đá, đống lốp, xác Hummer). Đầu 4 dốc để trống; mọi prop đặt y = h(x,z) và xoay theo pháp tuyến. `Dressing`: set piece ngoài rào (L1 48 món: tháp canh 4 góc, lều, container radio, trực thăng, xe tăng, kho nhiên liệu 6 thùng phuy 2×3 cạnh máy phát, hàng hedgehog 5 cái dọc x = 33.5 phía đông và 4 cái dọc x = −33.5 phía tây, ụ bao cát mỗi hướng; đã bỏ vòng thùng/lốp lẻ sát rào — vai "nhìn qua tường luôn có gì đó" giao cho cỏ). L2 dressing 21 món nằm trên vành đồi và xoay theo pháp tuyến: xác trực thăng trên pad (14, −41), trại (lều Tent_010 + trạm radio + thùng phuy + thùng) trên pad (−42, 22), xác Hummer và xe tăng nghiêng trên sườn đồi phía đông, hàng 5 hedgehog dọc x = 33.5 và 3 hedgehog dọc z = 33.5, 2 ụ bao cát + súng máy sát chân đồi. `Scatter`: cỏ khô/xương rồng không collider, **L1 564** (186 trong sân: Grass_003 rải khắp làn trống + dày dọc mép trong rào, Grass_008 scale 0.45–0.6 ôm rào để không che zombie, 10 Cactus_001; 378 ngoài rào: bụi rậm 28–34 m rồi bụi lớn + xương rồng tới 48 m); **L2 458** (158 trong sân: Grass_003 rải khắp nền thấp tránh lòng dốc ±3.6 m, dải mép plateau và footprint các cụm, Grass_008 scale 0.5–0.7 sát tường, 12 Cactus_001 + 6 Cactus_002; 300 ngoài rào 31–56 m trên vành đồi, tất cả xoay theo pháp tuyến địa hình). Cỏ L1 sinh bằng `System.Random` seed 20260906 với vùng loại trừ là footprint các khu + tâm sân r 6 m + dải rào, đặt y 0.01 trong sân (trên tile bê tông); tool sinh không giữ lại — muốn rải lại thì viết snippet tạm qua MCP.
- **Các node nhóm `Borders`/`Fence`/`Rocks`/`Obstacles`/`Dressing` phải ở layer Obstacle và mang `NavMeshModifier` overrideArea = Not Walkable, applyToChildren.** `NavMeshSurface` chỉ nhận modifier có layer nằm trong mask của nó — để node nhóm ở Default là modifier bị bỏ qua âm thầm, nóc thùng/đá/tháp thành đảo walkable và `SamplePosition` bán kính 5 m của spawn có thể thả zombie lên đó (đã xảy ra lần bake đầu: NavMesh lan tới đỉnh tháp 12 m).
- NavMesh bake theo *mặt* collider, không theo thể tích: prop kín cao ≥ 2 m có sàn walkable **bên trong** vì khoảng trống tới nóc đủ cho agent (lều 3.5 m từng có túi 60 m² không nối với đâu). Vì vậy các prop cao mang sẵn `NavMeshModifierVolume` Not Walkable trong prefab của chúng (danh sách ở bảng Model); prop thấp hơn 2 m tự carve.

Quy trình sửa map / bake lại: mở prefab (`PrefabUtility.LoadPrefabContents` hoặc Prefab Mode), sửa, lưu (fileID của root/`LevelMap`/`PlayerSpawn` phải giữ vì `LevelDefinitionSO._mapPrefab` trỏ vào component `LevelMap`); instantiate vào scene, `NavMeshSurface.BuildNavMesh()`, `AssetDatabase.CreateAsset(navMeshData, "Prefabs/Environment/NavMesh_Map_*.asset")` (đè cùng path, GUID giữ nguyên), gán lại `m_NavMeshData` trên prefab asset, xoá instance. Sau bake phải kiểm tra: `NavMesh.CalculateTriangulation()` không có đỉnh nào |x|,|z| vượt tường hay y cao hơn sàn/plateau, và lưới điểm 2 m khắp sân `CalculatePath` về tâm đều `PathComplete` (lần cuối: L1 745/745 sau bố trí lại 2026-09-06, L2 832/832 sau khi đổi sang heightfield; với L2 thêm kiểm tra không có đỉnh NavMesh nào ở 0.95 < y < 2.8 ngoài làn dốc ±5.8 m — tức vách plateau không lọt vào NavMesh). Nếu ghi đè asset NavMesh bằng `EditorUtility.CopySerialized` thay vì `CreateAsset` thì phải đặt lại `name` của asset — CopySerialized chép cả tên object tạm và Unity sẽ warn "Main Object Name does not match filename". Preview không cần Play: camera tạm tại `player + (0, 14, −4.5)`, `Euler(72, 0, 0)`, FOV dọc 65.8° (= FOV ngang 40° ở 9:16), render ra RenderTexture rồi xem PNG.

Material nền: `Art/Materials/Ground_Outpost` (bê tông sân L1, tiling 2 trên tile 14 m → tấm 3.5 m), `Ground_Scrub` (đất ngoài rào L1, tiling 16 trên plane 112 m), `Ground_Hills` (đất L2, tiling 120/7 — heightfield L2 dùng UV = xz/120 + 0.5 đúng như plane cũ nên tile vẫn 7 m), `Hill_Rock` (đá, tiling 2 mỗi mặt), `Hill_Cliff` (bản copy `Hill_Rock` tiling 40 cho submesh vách của heightfield → tile 3 m) — đều Toony Colors Pro 2 Hybrid, `_BaseColor` tint lên texture xám. Texture `Art/Textures/Environment/Ground_Concrete.png` (2×2 tấm bê tông có khe + vết loang), `Ground_Dirt.png` (đất khô + sạn), `Rock_Noise.png` (facet đá): 1024², tileable, sinh thủ tục bằng numpy vì không pack nào có texture nền; **PNG là nguồn sự thật**, muốn đổi thì vẽ/sinh lại PNG (không có script sinh trong repo). `MapBorder.mat` (dải đen mép map cũ) đã xoá.

### Roguelike trong trận (ngoài spec P0, thêm 2026-09-05 theo yêu cầu user)

Tài liệu đầy đủ: **`docs/ROGUELIKE-SYSTEM.md`** (thiết kế 6 skill, bảng stat → nơi tiêu thụ, trường hợp biên, quy trình thêm skill mới). Phần dưới đây là bản rút gọn.

Giết quái **rơi XP orb** tại xác, player lại gần thì orb hút về người và **chạm mới cộng XP** (user 2026-09-06; trước đó XP cộng thẳng khi kill) → đầy thanh thì lên **battle level** (chỉ sống trong một lượt chơi, khác hẳn level tài khoản của Meta progression) → hiện popup chọn 1 trong 3 skill. Hai loại chung một popup: **8 passive** (stat modifier, **mỗi skill đúng một hiệu ứng** — user 2026-09-05 tách 4 thẻ đa hiệu ứng thành thẻ đơn; 2026-09-06 bỏ ba thẻ bom EXTRA PAYLOAD / BIG BLAST / DEMOLITIONIST vì vô nghĩa khi chưa có AUTO GRENADE, nên ba `StatId` bom vẫn được `BombThrower` đọc nhưng không passive nào cấp) và **6 active tự kích hoạt** (AUTO GRENADE ném volley theo cooldown, ORBIT BLADES lưỡi cưa xoay quanh người, MOLOTOV ném chai lửa để lại vùng cháy, CHAIN LIGHTNING sét nhảy giữa các zombie, SHOCKWAVE sóng xung kích hất bay đám đông khi bị vây, SENTRY DRONE drone bay bên vai tự bắn — 4 skill sau thêm 2026-09-05 theo duyệt của user). **Không có nút bấm nào** — cơ chế ném bom thủ công đã bỏ hẳn (user 2026-09-05); quả bom chỉ còn tồn tại trong AUTO GRENADE để giữ spec §6. Data ở `Data/Roguelike/` (`RoguelikeSettings.asset` + 14 `Skill_*.asset` + `XpOrb.asset`), XP mỗi loại quái ở `ZombieDefinitionSO._xpReward` (Walker 10 / Runner 14 / Brute 30 / Giant 90) — giá trị đó nằm trên orb, nên tổng XP một lượt không đổi so với khi cộng thẳng, chỉ là phải đi nhặt. **XP orb**: `Roguelike/XpOrbManager` (trên `Systems/Managers`) nghe `ZombieManager.OnZombieKilled`, lấy `Prefabs/Pickups/XpOrb.prefab` từ pool (`Pools/XpOrbPool`, prewarm ở `Awake` vì không cần NavMesh), thả tại `zombie.Position` với cung bật lên + rải ngẫu nhiên rồi lơ lửng thở; **một `Update` duy nhất** tick mọi orb (orb không có `Update`): trong `_magnetRadius` 2.5 m orb bay về player với gia tốc, vào `_collectRadius` 0.45 m thì gọi `RoguelikeDirector.CollectXp(value)` (nhân `XpGain` tại đây) và trả pool. Số liệu ở `Data/Roguelike/XpOrb.asset` (`XpOrbDefinitionSO`: pool 96, scatter/pop/hover/bob/spin, magnet/collect/fly, VFX + clip nhặt để trống = im lặng). Orb đứng yên khi flow không `Playing`, chỉ hút khi player còn sống, `OnRunStarted`/`OnLevelEnded` trả hết về pool. Heal-per-kill vẫn tính ngay lúc kill.

**Passive = thuần stat modifier.** `PassiveSkillSO` chỉ chứa danh sách `StatModifier { StatId, Additive|Multiplicative, giá trị mỗi cấp }` — theo quy ước hiện tại mỗi asset đúng **một** modifier. `PlayerStatSheet` (trên `Player`) gộp mọi stack đang sở hữu thành 12 stat sống: multiplicative nghỉ ở 1, additive nghỉ ở 0. Vì vậy **thêm passive mới = thêm một `.asset`, không sửa dòng code nào** (`CODE-RULE.md` §7 Open/Closed). Hệ quả: nếu cần một hiệu ứng không biểu diễn được bằng stat (ví dụ nổ dây chuyền khi kill) thì phải thêm `StatId` mới + đúng một chỗ đọc nó.

**Active = một subclass `ActiveSkillSO` + một asset.** Cả hai loại kế thừa `SkillDefinitionSO` (identity + `MaxStacks` + `Apply()`), nên `SkillDraft`/popup/hàng sao dùng chung không phân biệt. `RoguelikeDirector.RebuildLoadout` là **một vòng lặp đa hình**: mỗi skill tự `Apply()` vào `PlayerStatSheet` (passive) hoặc `AbilityRunner` (active) — không có `if (là passive)`. `AbilityRunner` (trên `Player`) giữ slot `(skill, stacks, cooldownRemaining)` và tick tập trung; **SO là asset dùng chung nên tuyệt đối không giữ cooldown trong SO**. `Equip` lại skill đã có giữ nguyên cooldown đang chạy, nếu không bốc bất kỳ thẻ nào cũng tặng một lần kích hoạt miễn phí. `CooldownFor <= 0` = ability liên tục (lưỡi cưa, drone): runner không tick, nó làm việc trong `OnEquipped` (idempotent) và tắt trong `OnUnequipped` khi lượt mới bỏ nó. `CanTrigger(ctx, stacks)` (virtual, mặc định true) được runner hỏi mỗi frame **sau khi** cooldown về 0: trả false thì cooldown giữ ở 0 và hỏi lại frame sau — CHAIN LIGHTNING chờ có mục tiêu auto-aim, SHOCKWAVE chờ ≥ `_minZombiesToTrigger` zombie trong bán kính; không có nó thì một lần kích hoạt bị đốt vào khoảng không. Công thức cooldown chung nằm ở `ActiveSkillSO.ScaledCooldown(base, perStack, min, stacks)`. `AbilityContext` là file duy nhất phình khi active mới cần tới một hệ chưa ai dùng — hiện gồm `Player`, `Bombs`, `Blades`, `Molotovs`, `Lightning`, `Shockwave`, `Drone`. **Cách nhận**: `RoguelikeSettings._startingAbility` để trống (vào trận chỉ có súng); lần level-up đầu chỉ bốc từ `_firstLevelPool` (cả 6 active) để lượt nào cũng có ability sớm, sau đó trộn chung.

Mỗi active = **một SO subclass + một controller author sẵn trong `GameplayRoot.prefab`** (SO stateless, controller giữ mọi state và ref scene). Controller nằm **trên `Player`** chỉ khi hiệu ứng của nó phải xoay cùng nhân vật (ring sóng xung kích, điểm ném); thứ gì **không được ăn rotation của player** (sét, drone — user 2026-09-05; lưỡi cưa — user 2026-09-06, vòng cưa từng lắc theo mỗi cú quay người) nằm dưới node **`GameplayRoot/Abilities/`** ngang hàng `Player` và chỉ đọc vị trí player qua ref `_anchor`. `AutoBombSkillSO` → `Weapons/BombThrower`; `OrbitBladesSkillSO` → `Abilities/OrbitBlades` (`OrbitBladesController`, `transform.position = _anchor + up × _height` mỗi Update); `MolotovSkillSO` → `Weapons/MolotovThrower` (+ `MolotovDefinitionSO` cho phần không đổi theo stack: pool, tầm ném, thời gian bay, prefab `FireZone`, clip; struct `MolotovBurn` chốt bán kính/thời lượng/damage lúc ném nên lên cấp giữa lúc chai đang bay không đổi vùng lửa; **lửa của player không đốt player**, `_burnMask` = Enemy); `ChainLightningSkillSO` → `Abilities/Lightning` (`ChainLightningCaster`: 8 `LineRenderer` `Bolt1..8` author sẵn = trần `1 + jumps`, `useWorldSpace` nên hình bolt đứng yên trong world; material `Art/Materials/LightningBolt.mat` là bản copy `Tracer.mat` đổi `_BaseColor` sang cyan; nhảy bằng `OverlapSphereNonAlloc` quanh nạn nhân trước, không cần LOS; `DamageSource.Shock`); `ShockwaveSkillSO` → `Player/ShockwaveEmitter` (ring Cylinder dẹt material `WFXM_M_GlowCircle Add` tween scale bằng timer, lực 7 ≥ ngưỡng 4 nên hất bay mọi thứ trừ Giant, dùng chung `CinemachineImpulseSource` Bump 0.2 s với bom); `SentryDroneSkillSO` → `Abilities/Drone` (`SentryDroneController`: **bay vòng quanh player trong world space theo đồng hồ riêng** — `_orbitRadius` 1.4 m, `_orbitDegreesPerSecond` 60, `_hoverHeight` 1.7 + bob 0.12 m/1.2 Hz, `SmoothDamp` 0.2 s bám theo `_anchor` = `Player` nên player xoay tại chỗ drone vẫn đứng nguyên và luôn di chuyển; đặt vị trí trong `LateUpdate`; quay `Body` về mục tiêu, không có mục tiêu thì quay theo hướng bay; `Body` dựng từ primitive Hull/Glow/Barrel + `Muzzle`; bắn qua `ProjectileManager.Spawn` với `Data/Weapons/Gun_Drone.asset` — súng riêng, **không** ăn passive `WeaponDamage`/`FireRate` lẫn shop; tự quét 10 m mỗi 0.2 s, phạt `_sharedTargetPenalty` 4 m cho con mà player đang bắn để che sườn).

Nơi tiêu thụ từng stat — đây là danh sách đầy đủ, thêm `StatId` mới thì bổ sung vào đây:

| StatId | Đọc ở đâu |
|---|---|
| `WeaponDamage`, `FireRate`, `Knockback`, `ProjectilePierce` | `WeaponController.Fire` → gói vào struct `ShotStats` cho `ProjectileManager`/`Projectile` |
| `MoveSpeed` | `PlayerMotor.FixedUpdate` |
| `MaxHealth`, `HealPerKill` | `PlayerHealth` (`HandleStatsChanged` cộng luôn máu vào, `Heal`) — heal/kill do `RoguelikeDirector` gọi |
| `ExtraBombsPerVolley`, `BombRadius`, `BombDamage` | `BombThrower.ThrowVolley` (số quả thêm mỗi volley, bán kính) / `Explode` (damage) — nhịp ném là của `AbilityRunner`, `BombThrower` không còn charge/cooldown |
| `XpGain` | `RoguelikeDirector.CollectXp` (gọi từ `XpOrbManager` khi nhặt orb) |

⚠️ **Thứ tự nhân với Meta progression**: hệ số roguelike nhân **lên trên `gun.Stats`** (đã bao gồm cấp nâng cấp mua bằng coin), không nhân lên `definition`. Nhân nhầm chỗ là vô hiệu hoá toàn bộ shop.

**Vòng đời một lần level-up**: `RoguelikeDirector.Update` thấy có nợ level-up (và flow đang `Playing`) → `SkillDraft.Roll` bốc N thẻ **khác nhau**, bỏ skill đã max → `OnChoiceOffered` → `GameplayUiBinder` dựng `SkillCardData` rồi gọi `GameFlowController.PauseForLevelUp()` (state **`LevelUp`**, `timeScale = 0`) và `UIManager.ShowSkillChoicePopup`. Người chơi chạm thẻ → popup `Close()` → `OnClosed` → `RoguelikeDirector.ChooseOffer(index)` cộng stack, rebuild stat sheet. Còn nợ thì mở luôn bộ thẻ kế tiếp; hết nợ mới `OnChoiceClosed` → `ResumeFromLevelUp()`.

- Director **không tự dừng game**: nó chỉ phát event, flow mới đổi state. Nhờ vậy `PlayerMotor`/`WeaponController`/`WaveDirector`/`ZombieManager` tự đứng im vì đều gate sẵn trên `State == Playing`, không phải sửa gì.
- `LevelUp` là state riêng chứ không mượn `Paused`, nếu không `GameplayUiBinder` sẽ mở popup Pause đè lên.
- Popup chọn skill tắt cả `_closeOnBackKey` lẫn `_closeOnBackdropClick` — bắt buộc phải chọn.
- **Layout thẻ**: 3 cột dọc xếp ngang (tên → badge NEW → icon → mô tả → hàng sao), theo mẫu UX user đưa. **Mỗi thẻ có frame riêng** (`Frame_BannerFrame04_Navy01`, cùng frame với thẻ vũ khí, 300×720, cách nhau 30 px) và **popup không còn khung nền chung** (`Panel` giữ `Image` nhưng tắt, chỉ còn banner LEVEL UP + chip battle level nổi trên backdrop) — user 2026-09-06: ba thẻ dùng chung một nền nhìn không tách nhau. Image của thẻ cũng là `targetGraphic` của Button nên frame tự tối đi khi bấm. Hàng sao thay cho chữ "LV 3/5": số sao vàng = cấp **sau khi chọn**, tổng số sao = `MaxStacks`, nên nhìn là biết skill còn sâu bao nhiêu. Prefab author sẵn **5 slot sao** (trần sâu nhất trong pool); skill nông hơn ẩn bớt và `SkillCardView.DrawStars` dịch cả hàng lại cho vẫn cân giữa — thêm skill có `MaxStacks > 5` thì phải thêm slot sao vào mảng `_stars` của cả ba `SkillCardView` trong `SkillChoicePopup.prefab`, view sẽ log error nếu quên. Badge NEW hiện khi `SkillCardData.IsNew` (tức chưa sở hữu stack nào); ngược lại hiện badge **UPGRADE** (`Label_Tag01_Green`, chữ trắng) — skill đã học vẫn được `SkillDraft` chào lại cho tới khi chạm `MaxStacks`, và user (2026-09-06) muốn thẻ nói rõ đó là nâng cấp chứ không phải thẻ mới. Hai badge cùng vị trí, view bật đúng một cái.
- Thẻ có **chip loại** `PASSIVE`/`ACTIVE` dưới tên (đổi sprite tag theo loại, không tint) và đĩa icon tô cyan cho active — `SkillCardData.IsActive` ← `SkillDefinitionSO.Kind`. Thiếu nó hai loại nhìn y hệt nhau (user phản hồi 2026-09-05).
- Mô tả skill phải **ngắn, mỗi dòng một hiệu ứng** ("Damage +20%
Knockback +15%") vì cột chỉ rộng 300 px — văn xuôi dài sẽ tràn.
- Mọi skill đã max thì `SkillDraft.Roll` trả 0, director xoá nợ và không hỏi nữa.

Đường cong XP: `base 80 × 1.35^(level-1)`, trần battle level 12. Đo bằng nhịp kill thật của L1 (~280 kill/lượt ≈ 4090 XP) thì một lượt đi hết được **battle level 10, tức 9 lần chọn skill** — khoảng 20 giây một lần. Tổng max stack là 33 (8 passive) + 30 (6 active × 5) = 63 ≫ 11 lần chọn nên không bao giờ full được, lựa chọn luôn có sức nặng.

Wiring roguelike trong `GameplayRoot.prefab` (gán tay, không còn tool): `XpOrbManager` trên `Systems/Managers` (`_definition` = `Data/Roguelike/XpOrb.asset`, `_flow`, `_zombies`, `_rogue`, `_playerHealth`, `_player` = `Player` (chỉ đọc position), `_poolParent` = `Pools/XpOrbPool`, `_vfx`, `_audio`); `XpOrb.prefab` = root `XpOrb` (component `XpOrb`, không collider) + con `Gem` Cube 0.22 xoay (45, 0, 45) material `WFXM_M_GlowPalet Add`, không đổ bóng; `PlayerStatSheet` trên `Player`; `AbilityRunner` trên `Player` (`_flow`, `_health`, `_bombs`, `_blades`, `_molotovs`, `_lightning`, `_shockwave`, `_drone`); nhánh `Abilities/OrbitBlades` (`OrbitBladesController`: `_flow`, `_zombies`, `_anchor` = `Player`, `_height` 1, `_enemyMask` = Enemy, `_pivot` = con `Pivot`, `_blades` = 6 con `Blade1..6` — Cylinder dẹt, material `WFXM_M_GlowPalet Add`, author inactive); `MolotovThrower` trên `Player` (`_definition` = `Data/Weapons/MolotovDefinition.asset`, `_prefab` = `Prefabs/Weapons/Molotov.prefab`, `_bottlePoolParent` = `Pools/MolotovPool`, `_firePoolParent` = `Pools/PlayerFirePool`, `_throwOrigin`, `_aim`, `_motor`, `_health`, `_flow`, `_zombies`, `_audio`, `_burnMask` = Enemy); nhánh `Player/Shockwave` (`ShockwaveEmitter`: `_zombies`, `_vfx`, `_audio`, `_impulseSource` = source Bump 0.2 s, `_enemyMask`, `_ring` = con `Ring` inactive, `_burstVfx` = `Vfx_ZombieDeathSmoke`, `_clip`); node **`GameplayRoot/Abilities`** (ngang hàng `Player`, active) chứa nhánh `Lightning` (`ChainLightningCaster`: `_aim`, `_zombies`, `_origin` = `Player/AimOrigin`, `_enemyMask`, `_vfx`, `_audio`, `_bolts` = 8 con `Bolt1..8` LineRenderer world-space inactive, `_hitVfx` = `Vfx_ImpactFlesh`, `_zapClip`) và nhánh `Drone` (`SentryDroneController`: `_flow`, `_health`, `_playerAim`, `_zombies`, `_projectiles`, `_vfx`, `_audio`, `_enemyMask`, `_obstacleMask` = Obstacle, `_anchor` = `Player`, `_body` = con `Body` inactive ở local 0, `_muzzle` = `Body/Muzzle`); `RoguelikeDirector._abilities` → runner; `RoguelikeDirector` trên `Systems/Managers` với `_settings` = `Data/Roguelike/RoguelikeSettings.asset`, `_flow`, `_zombies`, `_stats`, `_playerHealth`; `_stats` nối cho `PlayerHealth`/`PlayerMotor`/`WeaponController`/`BombThrower` và `_rogue` cho `GameplayUiBinder`. Thêm passive mới = tạo asset qua `Create ▸ Zombie War ▸ Passive Skill` rồi kéo vào `_skillPool` của `RoguelikeSettings.asset`. Thêm active mới = subclass `ActiveSkillSO` + controller MonoBehaviour author sẵn dưới `Player` (nếu phải xoay cùng nhân vật) hoặc `Abilities/` (nếu không được ăn rotation của player; mọi mesh/renderer nó cần đều author trong prefab, inactive) + thêm field vào `AbilityContext`/`AbilityRunner` + asset qua `Create ▸ Zombie War ▸ Active Skill ▸ …` kéo vào `_skillPool` và `_firstLevelPool`. Icon skill là `Item_Icon_*` 256 px của Sci-Fi Survival đã copy vào `Art/UI/Sprites/Icons/` (bộ 64 px nhìn nhoè trên đĩa thẻ) — icon thuộc về skin UI, đổi skin thì phải với tới cả `_icon` của các asset skill cũ.

### Meta progression (ngoài spec P0, thêm 2026-09-05 theo yêu cầu user)

Spec docx cố ý không có metagame; user đã quyết định thêm sau khi P0 xong. Toàn bộ state nằm trong **`Core/ProfileService`** (trên `Systems/Managers`), số liệu tĩnh ở `Data/Rules/ProgressionRules.asset` (`ProgressionRulesSO`: XP/kill, bonus thắng, coin theo điểm, đường cong XP lên cấp, coin khởi điểm) và mục `Upgrade` của mỗi `GunDefinitionSO` (max level, % damage/băng đạn/tốc bắn/nạp đạn mỗi cấp, giá gốc và hệ số tăng giá).

- **Lưu**: PlayerPrefs qua `SaveService` (`zw_player_level`, `zw_player_xp`, `zw_coins`, `zw_gun_level_<id>`, `zw_gun_unlocked_<id>`). `ProfileService.EnsureLoaded()` đọc lười nên gọi từ `Awake` của hệ khác vẫn an toàn.
- **Áp vào gameplay**: `Gun.Stats` (`GunStats`: Damage/FireInterval) do `WeaponController.ApplyUpgrades()` gán trong `Awake` và khi `OnRunStarted`; `WeaponController`/`ProjectileManager` đọc `gun.Stats`, không đọc thẳng `definition` cho hai chỉ số này. Range, spread, pellet, VFX vẫn lấy từ definition.
- **Thưởng**: `GameFlowController.EndLevel` → `ProfileService.GrantRunRewards(kills, total, won)`; `LevelResult`/`ResultData` mang `CoinsEarned`/`XpEarned`, popup kết quả có hai hàng "COINS EARNED" / "XP EARNED".
- **Nâng cấp** chỉ qua `ProfileService.TryUpgradeGun(gun)` (kiểm tra unlock, max level, đủ coin); `OnChanged` báo cho `MenuUiBinder` vẽ lại header + trang vũ khí.

### Skill tree (ngoài spec P0, thêm 2026-09-05 theo yêu cầu user)

Tài liệu đầy đủ: **`docs/SKILL-TREE.md`**. Tab TALENT (placeholder) đã thành tab **SKILLS**: cây 13 node cố định (gốc + 3 nhánh Offense / Survival / Demolition × 4 tầng), mua **rank** bằng **coin** (cùng ví với shop súng), node chỉ mở khi mọi `_prerequisites` đã có ≥ 1 rank. Data ở `Data/SkillTree/` (`SkillTree.asset` — thứ tự node = thứ tự slot trên trang — và 13 `Node_*.asset`); mỗi node đúng **một** `StatModifier` mỗi rank, giá = `_baseCost × _costGrowth^rank`.

- **State**: `ProfileService.Skills` (`SkillTreeProgress`, PlayerPrefs `zw_skill_rank_<id>`), mua qua `ProfileService.TryUpgradeSkill(index)` → `OnChanged` → `MenuUiBinder.RefreshSkills` dựng `SkillNodeData[]` (mảng **mới** mỗi lần, vì trang diff rank cũ/mới để biết node nào animate).
- **Áp vào gameplay**: `PlayerStatSheet` có lớp `_permanent` (`ClearPermanent`/`AddPermanent`) được **áp lại trong `BeginRebuild()`**, nên rebuild của `RoguelikeDirector` sau mỗi thẻ không xoá mất bonus vĩnh viễn — director không cần biết skill tree. `Player/SkillTreeStatApplier` (trên `Systems/Managers`, `_profile` + `_stats`) nạp lớp này ở `OnEnable` và mỗi `ProfileService.OnChanged`.
- **UI**: `Page_SkillTree` trong `UIRoot.prefab` (index 3 của `MenuScreenView._pages` = `MenuTab.SkillTree`), hexagon `Btn_IconButton_Hexagon02_White1` tint theo màu nhánh, link là Image không sprite xoay theo góc cha→con, panel chi tiết dưới cây là **tầng khung duy nhất** của trang (`Frame_TableFrame01_Navy`). Animation: cây mọc từ gốc khi mở tab, glow thở ở node mua được ngay, ring + scale khi chọn, lắc khi chạm node khoá, punch + burst + pip nảy khi mua, node con tween từ màu khoá sang màu nhánh khi vừa mở. `FitToPage` co cả `Content` khi màn ngắn (không ScrollRect).
- Wiring `GameplayRoot.prefab`: `ProfileService._skillTree` = `Data/SkillTree/SkillTree.asset`; `SkillTreeStatApplier` trên `Systems/Managers`.

### Game feel (thêm 2026-09-05, ngoài spec P0)

Lớp cảm giác thuần presentation: logic gameplay chỉ phát event, các presenter dưới đây nghe và diễn. Số liệu nằm ở `Data/Rules/FeedbackProfile.asset` (nhóm Player / Hit Stop) hoặc field serialize trên chính component — không có số hình thức nào trong code.

| Thứ | Ở đâu | Việc |
|---|---|---|
| `Core/HitStopController` | `Systems/Managers` | Đóng băng / slow-mo theo **unscaled** time: bom nổ trúng ≥1 zombie (`BombThrower.OnExploded`; 0.07 s × timeScale 0.12), quái nặng chết (`ZombieDefinitionSO._deathHitStopDuration`: Brute 0.04, Giant 0.1, Walker/Runner 0 × 0.1), player chết (slow-mo 1.4 s × 0.25, chạy trong state `Lost` để zombie sau popup còn nhúc nhích chậm). Chỉ nhận yêu cầu ở `Playing`/`Lost`; khi flow đổi state nó buông và **chỉ trả `timeScale` về 1 nếu giá trị hiện tại vẫn là của nó** — Pause/LevelUp ghi 0 luôn thắng, Retry reload scene nên `Awake` của flow reset |
| Post-process | `Volume` global trên `CameraRig`, profile `Art/Rendering/GameplayPostProcess.asset` (Tonemapping Neutral; ColorAdjustments contrast 8 / saturation 12; Bloom threshold 1 / intensity 0.45 / scatter 0.6 / HQ off; Vignette đen 0.2 / smoothness 0.45); `Main Camera` bật `renderPostProcessing` | Bloom là thứ làm muzzle, tracer, nổ bom và viền dissolve (đều HDR additive) toả sáng. Máy yếu thì tắt Bloom trong profile; chỉ Vignette là bắt buộc vì presenter dưới cần nó |
| `Core/HitVignettePresenter` | `CameraRig` | Đọc `Vignette` từ `Volume.profile` (bản copy runtime, không bẩn asset). Spike lên `_playerHitVignetteIntensity` 0.45 khi `PlayerHealth.OnDamaged` rồi tắt dần trong `_playerVignetteDuration` 0.18 s (spec §12.1); dưới `_lowHealthFlashThreshold` thì thở theo sin tới `_lowHealthVignetteIntensity` 0.38 với tần số `_lowHealthPulseFrequency` 1.2 Hz. Chạy unscaled, chỉ ghi khi giá trị đổi |
| `Core/CameraLookAhead` | `CameraRig` | Vcam follow child `Player/CameraTarget`; script đặt target = player + offset SmoothDamp theo `PlayerMotor.WorldMoveDirection × NormalizedSpeed` (`_maxOffset` 1 m, `_smoothTime` 0.4). Đặt `_maxOffset = 0` là về camera tâm |
| `Player/PlayerHitReactionPresenter` | `Player` | Khi bị cắn: phát `PlayerDefinitionSO._hitVfx` (`Vfx_PlayerBlood`) tại `AimOrigin` quay theo hướng đẩy, impulse camera riêng (`_hitCameraImpulse`), `DOPunchRotation` `ModelPivot` nghiêng ra xa kẻ cắn (`_joltDegrees` 12°, 0.22 s). Bỏ qua jolt khi đòn đó giết; `PlayerDeathPresenter` `DOKill` pivot trước khi tween ngã nên hai tween không giằng nhau |
| `Weapons/Gun` motion | prefab `Gun_*` | Recoil vừa lùi vừa hất nòng lên (`_recoilKickDegrees` 5°); đổi súng pop scale 0.7→1 OutBack 0.2 s. Chỉ ghi transform khi có chuyển động (`_pivotDirty`); `SetVisible(false)` reset về pose nghỉ đã cache ở `Awake` |
| `Weapons/Bomb` | prefab `Bomb` | `TrailRenderer` (material `Tracer`, 0.3 s, gradient cam → đỏ sẫm) `Clear()` khi lấy/trả pool để không vệt từ vị trí pool; vòng telegraph thở (`_telegraphPulseAmplitude` 0.05, `_telegraphPulseFrequency` 3 Hz — hạ từ 0.08/5 Hz ngày 2026-09-06 vì user thấy giật/khó chịu) trong `TelegraphLead` cuối. Material `Art/Materials/BlastTelegraph.mat` (TCP2 Hybrid) dùng texture radial gradient **`Art/Textures/VFX/BlastTelegraphRing.png`** (256², sinh thủ tục bằng numpy — center trong suốt, dải sáng mờ quanh mép đúng bán kính nổ, mờ dần hẳn ra ngoài) thay vì đĩa đặc phủ kín cả `_ringDiameter`: đổi ngày 2026-09-06 vì user thấy vùng đỏ "to quá so với particle" và "quá noise" so với hiệu ứng nổ Epic Toon FX mới (xem hàng Epic Toon FX ở mục Particle) — đĩa đặc alpha 0.35 đồng đều với biên cứng, cộng pulse 5 Hz, đọc như một mảng phẳng chớp nhanh chứ không phải cảnh báo mềm. `_BaseColor`/`_Color` của material để alpha 1, độ trong suốt giờ hoàn toàn do alpha kênh của texture quyết định (PNG là nguồn sự thật, không có script sinh trong repo) |
| Xác zombie bay | `ZombieController.Die(direction, launchForce)` | Đòn kết liễu có lực ≥ `_physicsKnockbackThreshold` (tức bom; Giant threshold 999 nên không bay) giữ Rigidbody dynamic, **bật lại collider** và chuyển root sang layer **`Corpse`(14)** rồi `AddForce` cùng công thức knockback — xác bay theo sóng nổ, đáp đất, dissolve như thường rồi `RestoreBody()` trả layer + kinematic khi despawn. Corpse va chạm với Default/Obstacle/Ground/Prop, **không** với Player/Enemy/Bomb/Corpse; các mask `_hitMask`/`_enemyMask`/`_blastMask`/`_burnMask` không chứa nó nên đạn, auto-aim, bom sau và lửa nhìn xuyên qua xác. Tên layer khai ở `ZombieManager._corpseLayerName` |
| Flinch theo hướng | `ZombieAnimator` layer Hit Reaction | Tham số int `HitSide` (`Enemies/HitSide`: Front 0 / Left 1 / Right 2, tính bằng dot(`transform.right`, hướng đạn) với ngưỡng 0.5 — đạn bay về phía phải zombie là trúng sườn trái) chọn `Hit` (HitReact_Head, speed 3) / `HitLeft` / `HitRight` (HitReact_LeftArm/RightArm 3 s, speed 4). Transition Empty→state có 2 điều kiện `Hit` + `HitSide Equals`; không tự retrigger khi đang flinch |
| NavMeshAgent | 4 prefab `Zombie_*` | `autoBraking = false`: agent không hãm tốc khi tới gần đích (đích chính là player) nên zombie lao thẳng vào tầm cắn thay vì trôi tới |
| Nhịp chân khớp tốc độ | `Player/PlayerAnimationPresenter` + state `Locomotion` của `SoldierAnimator` | Clip locomotion của Survivalist author cho tốc độ chạy **~5.7 m/s** (Walk_N ~2.2, Run_S lùi ~3.05 — đo bằng vận tốc bàn chân trụ), trong khi player chạy 4.0 m/s nên chân quay như tua nhanh. State `Locomotion` giờ có **speed multiplier parameter `LocomotionSpeed`** (float, default 1 — chỉ ảnh hưởng layer Base Locomotion, không đụng Shoot/Hit/Death); presenter mỗi frame tính `playback = tốc độ thật / tốc độ clip tại blend hiện tại` (tham chiếu nội suy Walk↔Run theo `NormalizedSpeed`, nghiêng về `_runBackwardClipSpeed` khi lùi), clamp `_minPlaybackSpeed`/`_maxPlaybackSpeed` 0.5–1.8 và damp 0.12 s. Vì mẫu số là `PlayerMotor.CurrentMoveSpeed` (đã nhân `StatId.MoveSpeed`) nên **buff tốc độ làm animation nhanh theo**, và dưới ngưỡng walk hệ số fade về 1 để clip idle giữ nhịp gốc. Đổi clip locomotion thì phải đo lại 3 số `_walkClipSpeed`/`_runClipSpeed`/`_runBackwardClipSpeed` |

Wiring bằng tay nếu phải dựng lại (không có tool): layer `Corpse` ở slot 14 + tắt 4 ô ma trận Corpse×{Player,Enemy,Bomb,Corpse}; `VolumeProfile` với 4 override trên; `CameraRig` gắn `Volume` (global, profile) + `HitVignettePresenter` (`_volume`, `_health`, `_feedback`) + `CameraLookAhead` (`_motor`, `_player`, `_followTarget`); child rỗng `Player/CameraTarget` và trỏ `VCam_Follow.Follow` vào nó; `Player` gắn `PlayerHitReactionPresenter` (`_health`, `_definition`, `_modelPivot`, `_bloodOrigin` = `AimOrigin`, `_vfx`, `_impulseSource` = source Bump 0.15 s riêng); `Systems/Managers` gắn `HitStopController` (`_flow`, `_feedback`, `_bombs`, `_zombies`); `Bomb.prefab` thêm `TrailRenderer` kéo vào `_trail`; `PlayerDefinition._hitVfx` = `Vfx_PlayerBlood`. Muốn bỏ một hiệu ứng thì gỡ component tương ứng, không cần sửa code.

## Kiến trúc UI

Kiến trúc uGUI + TextMeshPro + DOTween, không dùng UI Toolkit / MVVM / DI. Canvas tách theo tần suất rebuild, popup chạy qua stack, và **UI không giữ reference gameplay** — mọi ref bắc cầu nằm ở binder (xem bảng bên dưới).

**Toàn bộ UI nằm trong một prefab: `Assets/_ZombieWar/Prefabs/UI/UIRoot.prefab`.** Nó chứa mọi màn hình (menu, HUD, countdown, popup layer), kèm `EventSystem` và `AudioSource` riêng — nghĩa là **không có reference nào từ trong prefab trỏ ra ngoài**. `GameplayRoot.prefab` đặt một instance tên `UIRoot` bên trong nó; menu và HUD là hai canvas của cùng prefab đó, bật/tắt theo state của `GameFlowController`.

```
UIRoot.prefab               [UIManager]        ← hub duy nhất, không giữ ref gameplay
├── EventSystem             [EventSystem, InputSystemUIInputModule]
├── UiAudio                 [AudioSource]      ← UI tự phát tiếng tap của mình
├── Canvas_HUD      order 0   → TopBar (timer/kills/score/pause + health bar), Joystick (neo giữa đáy (0.5, 0), y = 300 — user đổi 2026-09-05, trước đó ở góc trái), GunButton
├── Canvas_Menu     order 5  [MenuScreenView]  → Header [MenuHeaderView] (level + XP bar, coin, gear) + Pages/{Page_Shop, Page_Weapon [WeaponPageView], Page_Battle [BattlePageView], Page_SkillTree [SkillTreePageView], Page_Locked} + TabBar [MenuTabBarView] 5 tab (SHOP / WEAPON / BATTLE / SKILLS / LOCK, tab LOCK khoá)
├── Canvas_Countdown order 10 [CountdownView]  ← không GraphicRaycaster, không chặn input
└── Canvas_Popup    order 20 [PopupManager]    → Backdrop + PausePopup + ResultPopup + SkillChoicePopup + WeaponDetailPopup + SettingsPopup (prefab, inactive)
```

### Binder — lớp trung gian giữ ref gameplay

UI **không** được giữ reference tới hệ thống gameplay. Mọi reference bắc cầu nằm trong binder sống ngoài prefab UI, và **chỉ trỏ vào trong** UI (không bao giờ ngược lại):

| Binder | Ở đâu | Giữ ref | Việc |
|---|---|---|---|
| `Core/GameplayUiBinder` | `GameplayRoot/Systems/Managers` | `UIManager`, `GameFlowController`, `PlayerHealth`, `WeaponController`, `CameraShakeController`, `RoguelikeDirector` | subscribe event gameplay → gọi setter của `UIManager`; đưa `LevelResult` thành `ResultData`; trao lệnh (`Pause`, `RequestSwitch`, `Resume`, `Retry`, `GoToMenu`) cho UI qua `BindGameplayCommands` |
| `Core/MenuUiBinder` | `GameplayRoot/Systems/Managers` | `UIManager`, `GameFlowController`, `ProfileService`, `LevelDefinitionSO[]` | khi flow vào state `Menu`: dựng `MenuHeaderData` + `LevelCardData[]` + `WeaponEntryData[]` (chỉ số hiện tại và sau nâng cấp) rồi gọi `ShowMenuScreen`; PLAY → `GameFlowController.StartRun(level)`; UPGRADE → `ProfileService.TryUpgradeGun`; `ProfileService.OnChanged` → `RefreshMenu` |

Bố cục menu bám theo ba ảnh tham chiếu user đưa 2026-09-05 (kiểu tower-defense casual), khoác skin Sci-Fi Survival:
- **Header** (`MenuHeaderView`): vương miện + "LEVEL n" + thanh XP "xp/next" bên trái, chip coin, nút bánh răng bên phải → `SettingsPopupUI` — xem "Settings" bên dưới.
- **Battle** (`BattlePageView` + `LevelCardView`): segmented NORMAL/HARD ở trên (HARD khoá, thuần visual, chưa có data), tiêu đề "CHAPTER n", khung artwork 520² (`LevelDefinitionSO._artwork`, rỗng thì giữ placeholder icon castle), hai mũi tên hai bên, tên level + chip best score, nút START to ở đáy, counter "i / n". Mặc định đứng ở chapter xa nhất đã mở.
- **Weapon** (`WeaponPageView` + lưới `WeaponCardView` 3 cột, **4 slot** author sẵn trong mảng `WeaponPageView._cards` — `WeaponCard1..4`, lưới 3 cột: x = −330 / 0 / 330, hàng cách nhau 410 px (2 thẻ thêm 2026-09-06 cùng Sniper/SMG, nhân bản từ `WeaponCard1`)): banner "UNLOCKED", mỗi thẻ có pill tên, icon trong khung, "LEVEL n", thanh tiến độ "n/max"; súng chưa mở hiện khoá + "COMING SOON". Bấm thẻ → **`WeaponDetailPopupUI`** (prefab `Popups/WeaponDetailPopup`): tên, icon to, level, mô tả (`GunDefinitionSO._description`), lưới 2×2 `WeaponStatCellView` (ATTACK/RATE kèm giá trị sau nâng cấp màu xanh, SHOTS = pellet, TYPE = SINGLE/SPREAD), giá coin, nút UPGRADE, dòng hint ("NEEDS n MORE COINS" / "MAX LEVEL REACHED" / "LOCKED"). `UIManager` giữ mảng `WeaponEntryData` đang hiển thị và index popup đang mở để `RefreshMenu` sau nâng cấp vẽ lại cả popup.
- **Skills** (`SkillTreePageView` + 13 `SkillTreeNodeView` + `SkillDetailPanelView`): cây kỹ năng vĩnh viễn mua bằng coin — xem "Skill tree" ở mục Hiện trạng và `docs/SKILL-TREE.md`.
- **Shop** placeholder "COMING SOON"; tab thứ 5 là **LOCK** (icon khoá, `MenuTabButtonView._locked`, không có badge riêng vì `_lockBadge` là optional). Tab mặc định là Battle (`MenuScreenView._defaultTab`). Trang không phải Battle được author inactive nên `Awake` của view chạy lần đầu khi mở tab; `Bind` chỉ đụng field serialized nên gọi trước `Awake` vẫn đúng.

- `UIManager` chỉ phơi ra setter (`SetHealth`, `SetScore`, `SetGun`…), lệnh mở popup, và `BindGameplayCommands` / `ShowMenuScreen(cards, onSelected)`. Nó không biết `GameState`, không biết `LevelDefinitionSO`, không biết `AudioService`.
- Dữ liệu vào UI là **struct trình bày**: `UI/ResultData`, `UI/LevelCardData`. Popup và thẻ level không bao giờ nhận object gameplay.
- View HUD (`HealthBarView`, `TimerView`, `ScoreView`, `GunHudView`, `XpBarView`, `CountdownView`) là presenter câm; nút nối runtime bằng `Init(callback)` / `Setup(callbacks)`, không có persistent onClick nào trên prefab.
- Toggle dạng công tắc dùng `UI/SwitchToggleView` bọc `Toggle` uGUI: `Toggle` chỉ fade được một graphic, trong khi switch của Layer Lab là frame + handle mỗi trạng thái, nên view bật/tắt hai nhánh visual `On`/`Off`. Sprite switch lấy từ `Sprite/Component/Toggle/` của pack (đã copy vào `Art/UI/Sprites/Toggles/`); kích thước track/handle chép theo chính prefab của pack (222×83 và 147×91).
- Popup mở/đóng chỉ qua `PopupManager` (stack + draw order + backdrop dùng chung + phím Back qua `Keyboard.escapeKey`). Mọi đường đóng đi qua `PopupBase.OnClosed` nên callback không thể bị bỏ sót. Tween popup dùng `SetUpdate(true)` vì mở khi `timeScale = 0`.
- ⚠️ **`DOPunchScale` phải `DOKill()` + gán lại rest scale trước khi punch** (rest scale cache ở `Awake`, punch lấy `restScale * hệ số` chứ không `Vector3.one * hệ số`). Punch là tween *tương đối*: gọi chồng lên một punch đang chạy thì tween mới lấy scale đang phình làm gốc và không bao giờ co lại. Sửa 2026-09-06 cho `ScoreView` / `TimerView` / `XpBarView` — chip kills punch mỗi mạng, giết nhanh hơn 0.25 s một con nên nó phình gấp nhiều lần và che kín màn hình (user báo lỗi kèm ảnh). `CountdownView` không dính vì đã `DOKill(true)` sẵn.
- **Health bar chạy bằng anchor** (`DOAnchorMax`), không `Image.Type = Filled` — sprite pill 9-slice sẽ bị cắt cụt đầu bo nếu dùng Filled, và anchor đọc đúng ngay frame đầu khi `CanvasScaler` chưa kịp size canvas.
- **CanvasScaler match theo WIDTH** (`matchWidthOrHeight = 0`, reference 1080×1920) ở mọi canvas. Popup cao gần bằng design height tự co lại bằng `PopupBase._fitPadding`.
- `AudioService` **không còn** `PlayUi`/`_uiVoice`: tiếng UI thuộc về UI (scene menu không có `AudioService` để mượn).

### Settings (dựng lại 2026-09-06 theo yêu cầu user)

Popup settings từ một hàng SCREEN SHAKE nay thành **4 hàng**: SOUND / MUSIC / HAPTIC / SCREEN SHAKE (`Canvas_Popup/SettingsPopup`, panel 880×600, mỗi hàng `Row_*` = icon 52 px + label + `SwitchToggleView`, hàng cách nhau 100 px, tâm cụm lệch xuống −33 để trừ phần banner tiêu đề đè lên mép trên panel). Icon lấy từ `Icon_FunctionIcons(x2)/64` của pack: `Icon_Volume_On`, `Icon_Music`, `Icon_Vibration`, `Icon_Camera` (copy vào `Art/UI/Sprites/Icons/`, tint bằng đúng màu label).

- **`Core/SettingsService`** (trên `Systems/Managers`) là công tắc duy nhất của sound/music/haptic, đúng vai trò mà `CameraShakeController` giữ cho shake. Nó đọc/ghi `SaveService` (`zw_sound`, `zw_music`, `zw_haptics` — ba key bool; cặp `zw_music_volume`/`zw_sfx_volume` dạng float cũ chưa ai dùng nên đã bỏ) và áp thẳng: SOUND tắt thì `AudioService.SetMuted(true)` (mute toàn bộ world voice) **và** mute `UIRoot/UiAudio` (tiếng tap của UI); MUSIC tắt thì mute `Systems/Music`.
- **`Systems/Music`** là `AudioSource` loop, `playOnAwake` off, **chưa có clip** — project vẫn không có nhạc nền, đây là chỗ cắm sẵn để khi có track thì chỉ gán clip + `Play()`.
- **HAPTIC hiện chỉ lưu cờ**: `SettingsService.HapticsEnabled` được popup đọc để vẽ, nhưng chưa hệ nào rung máy nên chưa có nơi tiêu thụ (user duyệt để tạm 2026-09-06). Khi thêm rung thật thì hỏi cờ này trước khi gọi `Handheld.Vibrate`.
- **Luồng UI**: popup không biết service nào cả — `SettingsPopupUI.Setup(in SettingsData, Action<SettingId, bool>)` nhận 4 giá trị và báo ngược lại **id hàng nào vừa đổi**; `MenuUiBinder.HandleSettingChanged` là chỗ duy nhất map `SettingId` → `SettingsService` / `CameraShakeController`. Thêm một setting mới = thêm giá trị vào `SettingId` + `SettingsData`, thêm một `SwitchToggleView` trong prefab, thêm một nhánh `case` trong binder.
- Popup **Pause trong trận vẫn chỉ có SCREEN SHAKE** (`ShowPausePopup(..., shakeEnabled, onShakeChanged)` không đổi) — muốn tắt tiếng giữa trận thì phải mở rộng popup đó riêng.

### Animation menu (thêm 2026-09-06 theo yêu cầu user)

Trước đó chỉ trang SKILLS có animation; bốn trang còn lại và header/tab bar đứng im. Mọi hiệu ứng dưới đây là **presentation thuần**, số liệu (thời lượng, ease, độ trượt, scale) đều là field serialize trên component trong `UIRoot.prefab` — muốn tắt một hiệu ứng thì gỡ component hoặc đặt số về 0, không sửa code.

- **`UI/MainMenu/MenuPageRevealer`** (component dùng chung, gắn trên `Page_Battle`, `Page_Weapon`, `Page_Shop`, `Page_Locked`): mảng `_items` là các `CanvasGroup` của từng khối trong trang, mỗi khối mờ dần + trồi lên + phóng nhẹ theo thứ tự mảng với `_stagger`. Rest pose (anchoredPosition + scale) đọc ở `Awake` và trả lại ở `OnDisable`, nên một tween bị kill không bao giờ để khối nằm lệch.
- **`MenuScreenView.ShowPage` tắt hết trang rồi bật lại trang đích** (trước đây `SetActive(i == tab)`). Trang mặc định đã active sẵn lúc menu bind, chỉ off-then-on mới làm `OnEnable` chạy lại — đó là điều kiện để mọi trang replay entrance mỗi lần mở tab.
- **Battle**: `LevelCardView.PlayEnter(step)` cho thẻ chapter bay vào — mở trang thì trồi lên (`_openRise`), bấm mũi tên thì vào từ phải (next) / trái (prev). Bind là tức thời còn tween chỉ chạy sau, nên bấm liên tục chỉ restart tween chứ không kẹt thẻ nửa chừng. `Glow` sau thẻ thở bằng **`UI/UiPulse`** (scale + alpha loop). ⚠️ `UiPulse` **không được gắn lên nút**: `UiButtonFx` `DOKill` target khi nhấn và vòng lặp sẽ không quay lại. Cùng lý do đó, `_target` của `UiButtonFx` trên hai mũi tên đã trỏ vào con `Icon` — nếu để trỏ vào chính nút thì một cú nhấn giữa lúc reveal sẽ kill luôn tween vị trí và bỏ nút nằm lệch dưới.
- **Weapon**: 4 thẻ pop lần lượt khi mở tab (`WeaponCardView.PlayReveal`, stagger ở `WeaponPageView._revealStagger`, hoãn một tick bằng `DOVirtual.DelayedCall(0f)` vì trang author inactive nên `Awake` của thẻ chưa chạy lúc `OnEnable`). Sau khi mua nâng cấp, `WeaponPageView` diff level cũ/mới và chỉ thẻ vừa lên cấp mới `PlayUpgrade`: punch scale + chữ LEVEL nảy + thanh tiến độ tween bằng `DOAnchorMax`. Thanh dùng `DOComplete` (không `DOKill`) khi tắt trang, nếu không nó đứng lại ở giá trị nửa chừng tới lần rebind sau.
- **Header**: thanh XP tween thay vì nhảy, coin đếm dần tới giá trị mới và chip coin punch. Lần vẽ đầu tiên luôn snap (`_drawn`), chỉ những lần đổi sau mới chạy số.
- **Tab bar**: `MenuTabButtonView.SetSelected` tween màu icon/label, fade `Highlight` (bật trước khi fade vào, tắt sau khi fade ra) và nhấc icon lên `_iconLift` kèm punch scale.

### Art UI (GUI PRO Kit - Sci-Fi Survival)

Cập nhật icon 2026-09-06: toàn bộ 14 skill roguelike và 13 node skill tree dùng item art 256 px trong `UI_Items` (thay pictogram 64 px trên tree). Orbit Blades dùng Knife, Blood Pact dùng Serum; các node dùng artwork theo hiệu ứng. `SkillCardView` giữ màu Image đã author, không tint artwork bằng AccentColor; 3 thẻ bật preserveAspect. Node bị khóa dùng tint xám nhẹ để còn đọc được item. Weapon card có frame 264×160, icon 240×132; popup vũ khí có frame 560×300, icon 536×236, đều preserveAspect. Các kích thước này thay thông số ô vuông ở đoạn lịch sử bên dưới. Thêm 5 sprite Knife/Scope/Military_Helmet/Gasoline/Serum bằng AssetDatabase.CopyAsset; `UI_Items` hiện 23 sprite, đã kiểm tra pack Android vẫn 1 trang 2048².

**Toàn bộ art UI đến từ đúng một pack: `Assets/ThirdParty/GUI PRO Kit - Sci-Fi Survival/`** (yêu cầu user 2026-09-05 — không lấy sprite/font UI ở bất kỳ pack nào khác). **Chỉ asset thực dùng mới được copy** sang `Assets/_ZombieWar/Art/UI/` (`Fonts/`, `Sprites/{Buttons,Frames,Popups,Sliders,Labels,Icons,Toggles,Backgrounds}` — 83 file) bằng copy trong Editor (`AssetDatabase.CopyAsset` / Ctrl+D trong Project window, không copy bằng file system) để giữ nguyên import settings, đặc biệt là **spriteBorder 9-slice**. Font UI là **Oxanium ExtraBold** (title/số) + **Ubuntu Bold** (body), cả hai là TMP SDF có atlas nằm trong chính file `.asset`.

4 icon súng (`Art/UI/Sprites/Icons/Item_Icon_Gun_{Rifle,Shotgun,Sniper,Smg}.png`, đổi 2026-09-06 theo yêu cầu user) lấy từ `ResourcesData/Sprite/Demo/Demo_ItemIcon` của chính pack Sci-Fi Survival: `Item_GunIcon_Large_Ar/Shotgun/Sniper` cho rifle/shotgun/sniper và `Item_GunIcon_01` (carbine ngắn viền đỏ) cho SMG — pack không có icon SMG riêng, còn `Large_Pistol` là súng ngắn nên lệch loại hơn. Chúng thay hẳn bộ `Art/Sprites/Icon_*.png` cũ (AssetPreview render từ FBX, đã xoá cùng thư mục `Art/Sprites`). **Icon của pack là ảnh ngang** (~1066×466) nên mọi `Image` vẽ chúng phải bật `preserveAspect`: thẻ vũ khí 132², popup chi tiết 260², nút đổi súng trên HUD 128² đặt ở y = −14 để không đè chữ tên súng.

**Sprite Atlas (thêm 2026-09-06, `CODE-RULE.md` §5.4)**: hai atlas V2 ở `Art/UI/Atlases/` (project để `Sprite Packer Mode = Sprite Atlas V2`, nên atlas pack lúc Play/build, không cần bấm tay). **`UI_Core.spriteatlasv2`** (76 sprite: mọi button, frame, label, slider, toggle, `Popup_Frame01_Navy1`, `Image_Glow_Circle01_White`, `Background_ScreenGlow`, 3 sprite joystick và toàn bộ pictogram `Icon_*` 64 px) — một trang 2048², Android override **ASTC 4x4** vì chứa frame 9-slice và cạnh sắc; **`UI_Items.spriteatlasv2`** (18 sprite `Item_Icon_*`: 14 icon skill/level 256 px + 4 icon súng) — một trang 2048², Android **ASTC 6x6** vì icon luôn được vẽ thu nhỏ. Cả hai: Include in Build, padding 4, **tắt Rotation và Tight Packing** (9-slice cần rect nguyên), không mipmap, sRGB, `bindAsDefault` nên `Image` cứ tham chiếu sprite gốc như cũ — không có `Image` nào phải trỏ lại. **Cố ý không vào atlas**: `Background_10` (1280×720, nền full-screen đứng một mình dưới đáy nên không batch được với ai, cho vào chỉ tốn chỗ) và 6 sprite không được scene/prefab nào tham chiếu (`Play_Joystick_Skill_00_n`, `Play_Joystick_Skill_Slider_01`, `Frame_TableFrame02_Navy`, `Popup_Frame04_Navy`, `Icon_Magazine`, `Icon_Reload` — rà GUID 2026-09-06, xoá được). Tách Core/Items thay vì một atlas 4096² để HUD trong trận chỉ cần 2 texture UI (Core + icon súng) và tổng bộ nhớ nhỏ hơn. **Thêm sprite UI mới thì phải kéo vào đúng atlas** (Inspector của atlas → Objects for Packing, hoặc `SpriteAtlasAsset.Add` qua MCP) — quên là sprite đó tự thành một draw call riêng mà không có warning nào. Kiểm tra pack: `SpriteAtlasUtility.PackAtlases` rồi đọc `SpriteAtlasExtensions.GetPreviewTextures` (internal, gọi qua reflection) — mỗi atlas phải còn đúng 1 trang; tràn sang trang 2 là batching gãy âm thầm, lúc đó bỏ bớt sprite ít dùng chứ đừng nâng max size.

⚠️ Bẫy của pack, đều đã trả giá:
- **Rất nhiều sprite có border phủ hết kích thước texture** (ví dụ `Popup_Frame01_Navy1` 441×709 border 222/352/219/357). Đó là chủ ý: vùng giữa rộng 0 pixel nên khi kéo giãn nó lấy màu tại đúng cột/hàng biên. Cứ để `Image.Type = Sliced`, `pixelsPerUnitMultiplier = 1`, kích thước tuỳ ý. Khi chọn sprite mới phải **kiểm tra cột được lấy mẫu có đặc không** — với fill của slider thì cột đó phải opaque, không thì thanh máu sẽ trong suốt ở giữa.
- `Frame_BarFrame_Top01_Navy` / `Frame_BarFrame_Top02_*` / `Frame_BarFrame_Bottom01_Navy` có border **dọc bằng nguyên chiều cao** — chúng là dải mảnh vẽ sát mép màn hình, kéo cao ra sẽ hỏng. Panel/bar cao dùng `Frame_TableFrame01_Navy` (top bar, header) và `Frame_BannerFrame04_Navy01` (tab bar, thẻ vũ khí) — hai cái này slice được cả hai chiều.
- `Btn_TextButton_Square01_*` (174×188) **không** slice theo chiều dọc — giữ chiều cao nút quanh 140–190 để bo góc không bị kéo méo.
- Pictogram `Icon_FunctionIcons(x2)` **chỉ giải nén sẵn bản 64 px** (128/256/512 còn nằm trong `.unitypackage`). Chỗ nào vẽ icon lớn hơn ~120 px thì lấy `Icon_ItemIcons(x2)/Shadow_256` thay vì phóng to pictogram: bom trên HUD, artwork level, 6 icon skill roguelike đều đang làm vậy.
- `Icon_Gold` là **vòng tròn rỗng phẳng** — chỉ đọc ra "đồng xu" khi tint màu `Warning`. Tint trắng là ra một chấm trắng vô nghĩa.
- `Label_Tag01_SkyBlue` gần như trắng, nên chữ trên nó phải là `Ink` (dùng cho banner LEVEL UP); các tag còn lại (Green/Red/Blue) dùng chữ `Paper`.
- Nút hành động trong trận (`Play_Joystick_*`) là **ảnh vuông**, khác với sprite tròn có bóng của pack cũ — rect phải vuông, đừng bù offset cho bóng.

**Prefab UI là nguồn sự thật** — các tool `Tools ▸ Zombie War ▸ UI` (import asset, rebuild `UIRoot.prefab`, install vào `GameplayRoot`) đã xoá theo quyết định user 2026-09-05 (xem "Không giữ editor tool sinh object"). Bảng màu và mọi `[SerializeField] private` đều đã gán sẵn trong prefab; không mở public setter chỉ để wire. Quy trình tay khi mở rộng:

- **Thêm sprite/font UI**: copy đúng file cần dùng từ pack sang thư mục loại trong `Art/UI/` bằng Unity (giữ import settings + border 9-slice); font TMP copy xong kiểm tra còn atlas. Không tham chiếu xuyên qua `ThirdParty/`. **Sprite mới phải thêm vào `UI_Core` (chrome/icon 64 px) hoặc `UI_Items` (icon item 256 px, icon súng)** — xem đoạn Sprite Atlas ở trên.
- **Thêm level**: `LevelDefinitionSO` mới vào `Data/Levels/` → kéo vào `MenuUiBinder._levels` trong `GameplayRoot.prefab`. Trang Battle là carousel một thẻ nên không cần slot UI.
- **Thêm súng** (quy trình đã dùng cho Sniper/SMG 2026-09-06): move FBX + 2 texture từ pack vào `Models/Weapons` / `Art/Textures/Weapons` bằng `AssetDatabase.MoveAsset`, copy `assault1.mat` rồi đổi `_BaseMap`/`_MainTex`/`_BumpMap`; `GunDefinitionSO` mới vào `Data/Weapons/` (`_icon` lấy trong `Demo_ItemIcon` của pack Sci-Fi Survival, copy sang `Art/UI/Sprites/Icons/` — xem mục Art UI); prefab `Gun_*` dẫn xuất từ `Gun_AssaultRifle.prefab` qua `LoadPrefabContents` → thay con `Model`, đặt lại `Muzzle` (trên trục nòng) và `GripLeft`, giữ nguyên `GripRight` + rotation hai grip (pose bàn tay cho `WeaponHandIk`) → `SaveAsPrefabAsset`; instance gắn dưới **`Player/ModelPivot/GunSocket`** của `GameplayRoot.prefab` (layer Player = 8 cho cả cây con) → kéo vào `ProfileService._guns` (giữ thứ tự theo tên) và `WeaponController._guns` (thứ tự = vòng xoay nút switch). Trang Weapon đã author sẵn **4** `WeaponCardView` trong `WeaponPageView._cards` (đủ cho 4 súng hiện có); súng thứ 5 trở đi phải nhân bản `WeaponCard1` trong `UIRoot.prefab`, đặt vào ô lưới kế tiếp và nối vào `_cards`, nếu không `WeaponPageView.Bind` log error "n weapons but only m authored slots".
- **Wiring `GameplayRoot.prefab` phải giữ**: `GameplayUiBinder` / `MenuUiBinder` trỏ vào `UIManager` của instance `UIRoot`; `LevelMapLoader._virtualCamera`; `ProfileService` (`_rules` = `Data/Rules/ProgressionRules.asset`, `_guns`) nối vào `GameFlowController._profile` / `WeaponController._profile` / `MenuUiBinder._profile`; object `Player` tắt sẵn.

Animator controller (`Animation/Soldier/SoldierAnimator.controller`, `Animation/Zombie/ZombieAnimator.controller`) chỉnh trực tiếp trong cửa sổ Animator (tool build từ recipe đã xoá cùng `AnimatorBuildRecipe.asset`). `SoldierAnimator`: 4 layer — Base Locomotion blend tree 2D, Upper Combat với `UpperBodyMask`, Hit Reaction, Full Body. `ZombieAnimator`: Base blend 1D theo **m/s** Idle/Walk/WalkFast/Run + Attack/Knockback/Death; layer Hit Reaction thân trên ở index 1 (`ZombieAnimationPresenter._hitReactionLayerIndex`). Layer Hit Reaction dùng Override (không Additive) vì clip flinch của pack là full pose. Soldier chưa có clip hit/death trong pack (không cần clip reload: game không có nạp đạn).

Zombie animation lấy từ pack **`Assets/ThirdParty/Zombie_Animations/`** (Humanoid, avatar `T_pose`): `Zombie_Idle_01`, `Zombie_Walk_01/Walk_Fast01/Run_01_Forward_InPlace` (ngưỡng 1.4/2.3/3.6 m/s), `Zombie_Attack01` (state speed 2 vì clip dài 2.33 s), `Zombie_HitReact_Head` cho Hit (mặt trước) và Knockback (speed 3), `Zombie_HitReact_LeftArm/RightArm` cho flinch hai sườn (speed 4), `Zombie_Idle_Death`. Năm clip one-shot đó đã tắt Loop Time trong import settings. Không dùng `Paired_*` (cần victim) và `Crawl_*`. `ZombieController` đẩy tốc độ agent thật (m/s) vào tham số `Speed`; `_attackWindup` 0.45 s cho Walker/Runner/Brute khớp thời điểm vung tay, `FeedbackProfile._deathPoseDuration` 0.6 s để clip ngã đọc được trước khi dissolve.

Layer vật lý đã thêm: `Player`(8) `Enemy`(9) `Obstacle`(10) `Ground`(11) `Prop`(12) `Bomb`(13) `Corpse`(14); ma trận đã tắt va chạm Player–Bomb và Corpse–{Player, Enemy, Bomb, Corpse}. `PlayerSettings.runInBackground` hiện **tắt** (`runInBackground: 0`, rà 2026-09-06): Play mode trong Editor đứng hình khi cửa sổ mất focus, nên test qua MCP phải focus cửa sổ Unity (hoặc set `Application.runInBackground = true` tạm trong phiên chơi).

Còn thiếu / biết trước: nhạc nền (project không có track nào), vỏ đạn văng và decal máu/cháy (spec §12.1, chưa có asset), âm nổ bom đang mượn `AntiMaterialRifle_far_01`. Thư mục `Captures/` (ảnh chụp qua MCP) đã gitignore.

## Build Android (sản phẩm APK)

Player settings đã có: scripting backend **IL2CPP**, kiến trúc **chỉ ARM64**, min SDK **25**, `productName: Zombie War`, `companyName: DucHien`, application id `com.duchien.zombiewar`, orientation **Portrait** khoá cứng. Build scene list: `Loading` (index 0) rồi `Gameplay`.

**Chưa có build script hay CLI**; build trong Editor (File → Build Settings → Android). `*.apk`, `*.aab`, `Builds/` bị gitignore — đưa APK lên **GitHub Release**, đừng cố commit. Nếu thêm method build trong Editor, dạng headless là:

```bash
Unity -batchmode -quit -projectPath . -buildTarget Android -executeMethod <Namespace.Class.Method> -logFile build.log
```

(dùng binary editor 2022.3.62f3). Skill `unity-build-tool` nhắm WebGL; phải chỉnh lại chứ đừng chép nguyên.

## Test

`com.unity.test-framework` đã cài, chưa có test assembly. Khi có asmdef EditMode/PlayMode:

```bash
Unity -batchmode -runTests -projectPath . -testPlatform EditMode -testResults results.xml
```

Đổi `EditMode` thành `PlayMode` khi cần. Logic C# thuần (công thức damage, nhịp wave, timer level) nên là class thường để test EditMode không cần scene.

## Unity MCP

`com.coplaydev.unity-mcp` là package dependency, nên điều khiển được Editor qua MCP (dựng scene, `validate_script`, `read_console`). Nếu thiếu tool `mcp__UnityMCP__*` hoặc sai port, dùng skill `unity-mcp-connect` — mỗi editor đang mở bind một port riêng và port đổi giữa các lần mở editor. `.mcp.json` đã nằm trong `.gitignore`. Compile-check sau mỗi lần sửa script (`CODE-RULE.md` §8).

**Không tự chụp màn hình / preview qua MCP** (yêu cầu user 2026-09-06): không gọi `manage_camera screenshot`, render preview ra RenderTexture/PNG rồi đọc ảnh, hay bất kỳ hình thức chụp-để-xem nào khi user chưa yêu cầu hoặc cho phép rõ ràng trong task đó. Verify bằng compile-check, `read_console`, `execute_code` trả text ngắn, đọc prefab/asset YAML. Nếu thấy cần nhìn ảnh mới kết luận được thì nói rõ và hỏi trước, đừng tự chụp.

## Git

- **Không tự ý thao tác git.** Không commit, push, tạo/xoá nhánh, merge, reset, stash hay bất kỳ lệnh ghi nào khi người dùng chưa yêu cầu và cho phép rõ ràng. Chỉ được đọc trạng thái (`git status`, `git log`, `git diff`).
- **Commit message ngắn gọn, bằng tiếng Anh**, một dòng theo dạng mệnh lệnh (ví dụ `Add zombie dissolve shader`). **Không** thêm dòng ghi nguồn hay tác giả (không `Co-Authored-By`, không "Generated with", không tên tool).
- Nhánh mặc định và release là **`main`**; làm việc hằng ngày trên **`dev`**.
- `Assembly-CSharp*.csproj` và `.sln` ở root do Unity sinh ra và bị gitignore — không sửa tay.
- `*.unitypackage` bị gitignore, nên các dòng xoá `.unitypackage.meta` lạc (ví dụ "Cat Demo URP" của Toony Colors) là nhiễu bình thường.
